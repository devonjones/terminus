# secrets — never commit, anywhere
*.pem
config.toml

# measured outputs / review artifacts from root-level runs (examples/ are kept)
/horizon_mask*.yaml
/horizon_mask*_profiles.json
/*.hrz
/*.stellarium.txt
*_frames/

# python
__pycache__/
*.pyc
*.egg-info/
.venv/
dist/
build/
.pytest_cache/

# measured horizon captures (large; kept on disk, not in git)
captures/
scratch/
