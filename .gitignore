# secrets — never commit, anywhere
*.pem
config.toml

# measured outputs / review artifacts from root-level runs (examples/ are kept)
/horizon_mask*.yaml
/horizon_mask*_profiles.json
/*.hrz
/*.stellarium.txt
*_frames/
# the run's own record and the page that inlines all of it: coordinates, clock
# times and hundreds of photographs of somebody's garden. *_frames/ was ignored
# while the page embedding every one of those frames was not.
/*_fiducials.jsonl
/*_polar.html

# python
__pycache__/
*.pyc
*.egg-info/
.venv/
dist/
build/
.pytest_cache/

# measured horizon captures (large; kept on disk, not in git)
captures/
scratch/
