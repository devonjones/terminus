{"id":"terminus-1","title":"Sky Safari horizon exporter (image-based PNG)","description":"Add to_skysafari_png(rows) exporter. Sky Safari's custom horizon is a rendered image, not a point list: PNG spanning azimuth 0-360 across width and altitude -90..+90 across height, horizon drawn as a line with everything below opaque. We already have the mask as az->altitude and depend on PIL, so render the silhouette at Sky Safari's expected dimensions/convention. Wire into export_all and CLI export. Third target after N.I.N.A. (.hrz) and Stellarium (.txt).","design":"PIL. Map az->x (0-360 across width), alt->y (-90 top .. +90 bottom or per SkySafari convention - verify). Interpolate the mask across full width. Opaque below the horizon line, transparent above.","acceptance_criteria":"PNG produced at correct dimensions; horizon line matches mask; loads in Sky Safari as a horizon; unit test on output size + a below/above-line pixel.","notes":"Cleaner-image option (Devon): instead of relying on the scope's blurry/narrow-FOV frames for the Sky Safari picture, let the user supply a phone PANORAMA of their horizon (like the ones Devon shot). The scope sweep provides the accurate az/alt horizon LINE; register/map the panorama to the measured points (horizon line + a few az/alt fiducials) to warp the pano into Sky Safari's 0-360 x -90..+90 image space. Result: a photo-real, in-focus, true-coordinate-aligned horizon image. So Sky Safari export has two modes: (a) synthetic silhouette rendered from the mask, (b) panorama-mapped photo. The scope stays the source of geometric truth; the pano is just prettier pixels.","status":"open","priority":2,"issue_type":"feature","owner":"devon.jones@gmail.com","created_at":"2026-08-02T14:47:15.328409152-06:00","created_by":"Devon Jones","updated_at":"2026-08-02T14:49:58.369150583-06:00"}
{"id":"terminus-10","title":"Sweep: sky reference goes stale across a multi-hour run","description":"CAUGHT LIVE 2026-08-03 mid-sweep, before it did damage.\n\nBUG. run_sweep seeds sky_ref ONCE from a near-zenith frame before the column loop, then passes that same value to every column. A full circle at 10 deg spacing takes 3-4 hours. Across twilight the sky itself changes by orders of magnitude while the reference stays pinned at its daylight value.\n\nCONSEQUENCE. The blocked test is 'median < 0.5 * sky_ref'. With sky_ref = 254 that threshold is 127. Once genuinely open sky falls below 127 — which it does shortly after sunset — EVERY remaining column reports blocked_above. The sweep would run to completion and return a mask claiming an entire hemisphere is obstructed, with no error and no warning. Same class as the auto-exposure failure: a stale reference silently makes everything look dark.\n\nEVIDENCE IT HAD NOT YET FIRED. At the moment of the catch, high-altitude samples read: az 0-30 lum 63-105 (the tall tree, correctly dark) and az 40-70 lum 248-253 (open sky, still matching the 254 reference). So columns 0-60 from part 1 are sound; the failure was pending, not past.\n\nFIX APPLIED. SKY_REF_MAX_AGE = 420 s. Inside the column loop, if the reference is older than that, re-point to (Sun azimuth + 180, alt 75), re-measure, and log 'sky reference refreshed: OLD -> NEW'. Failure to refresh keeps the old value and resets the timer rather than aborting the run. Costs one extra pointing per ~7 minutes, roughly 15% more wall clock.\n\nALSO WORTH DOING: log the reference alongside every column so a saved run can be re-judged with the reference that was actually in force, and consider deriving a per-column reference from that column's own brightest sample when it clearly contains sky (free, adapts automatically); the external reference is only strictly needed for fully blocked columns.\n\nRELATED: this is the third silent-failure-by-stale-or-unverified-state bug today (exposure lock not taking, goto not arriving, heater set_setting ignored). Worth a general rule in terminus: every state-changing scope call verifies its readback, and every cached measurement carries an age.","status":"open","priority":0,"issue_type":"bug","owner":"devon.jones@gmail.com","created_at":"2026-08-03T19:30:08.114212588-06:00","created_by":"Devon Jones","updated_at":"2026-08-03T19:30:08.114212588-06:00"}
{"id":"terminus-11","title":"Split responsibilities: scope measures altitude, photos identify obstructions","description":"ESTABLISHED 2026-08-03 from focus geometry plus Devon's site constraint.\n\nTHE SCOPE CAN NEVER RESOLVE A HORIZON FEATURE. Seestar S50 is 250mm f/4.9 focused at infinity, so the hyperfocal distance is H = f^2/(N*c) = 250^2/(4.9*0.003mm) ~ 4.3 km. Devon: 'from where the telescope is, there's literally no line of sight on anything farther than a house across the street except the sky' — every terrestrial target is within ~50 m, three orders of magnitude inside that limit. This is geometry, not a focus error, and no setting fixes it.\n\nMEASURED CONFIRMATION across 130+ scan frames: gradient p99 runs 0.73-1.34 (best single frame 2.55); a focused frame would be in the tens. The spread tracks CONTENT not range — the 'blurriest' frames are saturated open sky (lum 253.4, nothing to resolve) and the 'sharpest' are terrain with real brightness structure. There is no azimuth where the view sharpens.\n\nCONSEQUENCE FOR THE DESIGN. The two instruments are complementary, not redundant, and neither is a fallback for the other:\n  * SCOPE measures ALTITUDE — accurate, in true az/alt, at night, no registration needed. It cannot identify anything.\n  * PHOTOS identify TYPE — SegFormer already returns tree/building/fence/railing per pixel because phone frames are in focus. Their absolute orientation must be borrowed from the scope.\n\nACTION: the mask should carry BOTH — altitude from the scope, obstruction type from the photo segmentation, each labelled with its source. The existing obstruction_type() veg-vs-structure tag infers from colour blobs in a blurred frame and is unreliable by construction (it already degrades to 'structure' for everything after sunset); it should be demoted to a hint or dropped once photo-derived types are available.\n\nWHY IT MATTERS PRACTICALLY: a deciduous tree's horizon changes between summer and winter; a roofline does not. Knowing which columns are 'tree' tells the user which to re-measure seasonally and which are permanent — and that is a planning feature, not a cosmetic label.","status":"open","priority":1,"issue_type":"feature","owner":"devon.jones@gmail.com","created_at":"2026-08-03T19:48:48.948355457-06:00","created_by":"Devon Jones","updated_at":"2026-08-03T19:48:48.948355457-06:00"}
{"id":"terminus-12","title":"Scenery RTSP stream freezes on long runs, returning identical frames","description":"OBSERVED 2026-08-03 ~22:00 after ~3.1 hours of continuous scenery view (lapse_ms 11.2M).\n\nSYMPTOM. capture_rgb returns a BYTE-IDENTICAL frame regardless of pointing:\n  az 120 alt 70 -> p99 2.67 mean 0.76\n  az 300 alt 45 -> p99 2.67 mean 0.76\n  az 300 alt  5 -> p99 2.67 mean 0.76\nZenith and ground give the same numbers. get_view_state reports state 'working', mode 'scenery' — the scope believes it is fine. stop_view + start_view did NOT clear it.\n\nDAMAGE DONE. The western arc (az 260-350) was recorded as blocked_above 60 for 8 of 10 columns because every frame read 0.8. That fabricated ~100 degrees of obstruction out of open sky, and when those columns were added to the orientation fit it moved yaw from a stable 130 to 70, pitch to -17.7, tilt to 21.2, RMS 10.3. Devon spotted it immediately as implausible ('going up to 140 degrees from 40? only with new nighttime measurements? color me skeptical') — the fit blowing up was the confirmation.\n\nVERIFIED NOT the same as the earlier state: at ~20:45 the same pointings gave az 300 alt 50 = 11.25, alt 10 = 0.09, az 120 alt 50 = 5.52 — real, position-dependent values with >100:1 sky/terrain contrast. So night measurement WORKS; the stream degraded later.\n\nLIKELY CONTRIBUTORS: (a) a scenery view left running for hours, (b) concurrent control connections — the Seestar tolerates only ONE, and this session repeatedly attached diagnostic scripts while a sweep held the socket, which also produced 'cannot read current pointing' and a BrokenPipeError.\n\nFIXES NEEDED:\n  1. DETECT IT. Hash or compare consecutive frames: if a capture is identical to the previous one after the mount has moved, the stream is stale. Refuse to record a measurement rather than emitting blocked_above. Partial work exists — capture_rgb now retries when a frame is essentially black — but that does not catch a frozen NON-black frame, which is the general case.\n  2. RECOVER. Periodically restart the view on long runs, and on detection try stop_view/start_view then a full reconnect; if the frame is still identical, abort the run with a clear error instead of continuing.\n  3. SERIALISE ACCESS. One connection only. A sweep should hold a lock; diagnostics should read the log, not the scope.\n  4. INVESTIGATE exp_ms: get_view_state reports exp_ms 240.0 while get_setting reports isp_exp_ms 20.0. The setting we verify is not the exposure the camera uses, which also explains why lock_exposure appears to clamp every requested value. See seestar_alp for how the view's own exposure is set.\n\nDATA STATUS: part1 (az 0-70) and part2 (az 70-250 incl. refinement) are GOOD — 26 columns taken in daylight and twilight with position-dependent values. part3/part3b (az 260-350) are void and must be re-measured.","status":"open","priority":0,"issue_type":"bug","owner":"devon.jones@gmail.com","created_at":"2026-08-03T21:20:29.021970459-06:00","created_by":"Devon Jones","updated_at":"2026-08-03T21:20:29.021970459-06:00"}
{"id":"terminus-13","title":"Mask carries obstruction type from the instrument that cannot determine it","description":"SPOTTED BY DEVON 2026-08-03: 'note, 6 of the 9 are trees, so I think 2 models would be good here' — while the residual table showed all four large disagreements as 'structure'. The counts differ because two different labels are in play, and the one being displayed is the untrustworthy one.\n\nTHE BUG. sweep.obstruction_type() infers vegetation from green/yellow dominance in a scope frame. That frame is (a) a blur — 250mm f/4.9 focused at infinity, every terrestrial target three orders of magnitude inside the ~4.3 km hyperfocal distance — and (b) after sunset, a neutral silhouette. So at night it labels essentially everything 'structure', which is what the report showed. The mask stores that as the column's type and downstream code uses it.\n\nMeanwhile SegFormer on the in-focus phone frames returns real semantic classes (tree / building / fence / railing) and is the label Devon was reading off the imagery. His 6-of-9-are-trees is from the reliable source; the table was from the unreliable one.\n\nFIX: type comes from photo segmentation, altitude comes from the scope, and each field records its source. obstruction_type() should be demoted to a daylight-only hint or removed once segmentation types are available. This is the instrument split already filed separately — the scope measures where the boundary is and cannot say what it is.\n\nCONSEQUENCE FOR THE TWO-MODEL WORK. BOUNDARY_MODELS (tree vs structure: different repeats, run length, SNR gate, expected transition width, export buffer) currently keys off the scope's type, so at night every column silently gets the 'structure' model — including the tree columns that most need the vegetation one. Wiring the photo's class in is what makes the two-model split actually operate.\n\nALSO, AND NOT YET DONE: if 6 of 9 visible errors sit at tree boundaries, the same split is needed in the PHOTO pipeline. skymask.horizon_rows applies one rule (first sustained run of non-sky) to canopy and roofline alike. horizon_band already measures the canopy's porosity and extent; the extraction should use the vegetation-aware path where segmentation says tree, and the sharp-edge path elsewhere.","status":"open","priority":1,"issue_type":"bug","owner":"devon.jones@gmail.com","created_at":"2026-08-03T22:26:44.957801175-06:00","created_by":"Devon Jones","updated_at":"2026-08-03T22:26:44.957801175-06:00"}
{"id":"terminus-14","title":"find_edge noise estimate counts the sky gradient as noise","description":"FOUND 2026-08-03 23:50 while measuring bright western columns after polar realignment.\n\nBUG. find_edge estimates noise as the mean absolute difference between CONSECUTIVE samples. Under light pollution the open sky is not flat with altitude — skyglow brightens toward the horizon, measured at az 300 rising 36.9 at alt 60 to 73.7 at alt 11. That smooth gradient enters the noise estimate, inflating the denominator, so a real step is divided by the trend it sits on and fails the SNR gate.\n\nMEASURED ON FIVE SAVED PROFILES (step / noise_1stdiff / noise_2nddiff / snr_now / snr_fixed):\n  az 300  19.9   9.44  0.75   2.11  26.6\n  az 310  35.1   6.53  0.54   5.37  64.5\n  az 320  15.8  10.78  2.65   1.47   6.0\n  az 340   9.8   6.78  0.88   1.44  11.0\n  az 350   3.7   2.42  0.82   1.53   4.5\nTrue scatter is 0.5-2.7 counts; the current estimator reports 2.4-10.8. Four of five columns were rejected as 'no edge' purely because of this.\n\nFIX DIRECTION: estimate scatter from SECOND differences (median |x[i+2]-2x[i+1]+x[i]| / sqrt(6)), which is blind to any linear trend, or from residuals after removing the fitted two-level model.\n\nDO NOT SHIP THAT ALONE — IT IS THEN TOO PERMISSIVE. With a noise floor under 1 count, trivial steps clear the gate: az 350 'passes' at SNR 4.5 on a step of 3.7 counts against a sky reference of 19.7, i.e. a 19 percent wobble in the skyglow gradient rather than a horizon. A SECOND gate is needed on ABSOLUTE step size relative to the current sky reference (a step of >= ~25 percent of sky_ref would admit 300/310/320/340 and reject 350). The existing EDGE_SNR is scale-free and therefore cannot express 'were there enough photons'.\n\nALSO UNRESOLVED, DO NOT ADOPT THE NEW NUMBERS YET: with the fixed estimator az 340 and az 350 come out at 10.0 deg, while tonight's western sweep recorded both as blocked_above 60 and the photo mosaic puts them near 56. Three sources disagree. The saved frames for all of these exist under captures/2026-08-03-night-targeted/scan/, so this can be settled offline by inspection before any refit.","notes":"CORRECTION TO THIS TICKET (00:05, same night). The severity above is overstated and the diagnosis was partly wrong.\n\nThe trend-insensitivity argument is sound, but my demonstration used the MEDIAN of second differences, which is robust to large excursions — and in a scatter-only profile those excursions ARE the noise. That is the wrong kind of robustness: it discounted real scatter and made the gradient look responsible. It also broke the existing regression test (a 2026-08-02 auto-exposure column of pure scatter started passing as an edge), which is exactly what that test exists to catch.\n\nREDONE PROPERLY with RMS of second differences (trend-insensitive, but not outlier-insensitive), plus the absolute step floor:\n  az 200 -> 35.0  snr 3.25  step 22.4\n  az 310 -> 15.0  snr 4.10  step 35.1\n  az 300 -> no edge, snr 2.42 (marginal)\n  az 320/340/350 -> no edge, snr 1.2-1.4\n  az 10/30 -> no edge, NEGATIVE step: uniformly dark, correctly the tree\nFor az 300 the RMS second-difference noise is 8.2 against the first-difference estimate of 9.44 — near-identical. THE PROFILES ARE GENUINELY NOISY, NOT MERELY TRENDING. The fix recovers nothing.\n\nWHAT IS ACTUALLY TRUE: at a sky reference near 20 counts the measurements are too noisy for reliable edge detection except where the step is very large. Only the two strongest columns survive. This is the same limit that produced 'no edge' at az 180 and az 50 earlier — insufficient absolute signal, seen now in the bright northwest as well as the dark south.\n\nKEEP THE CODE CHANGE ANYWAY: trend-insensitive noise plus a minimum absolute step is more defensible than a first-difference estimate with no floor, and all 47 tests pass including the scatter regression. Just do not expect it to rescue faint columns.","status":"open","priority":0,"issue_type":"bug","owner":"devon.jones@gmail.com","created_at":"2026-08-03T23:49:38.757552156-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T00:03:49.519697759-06:00"}
{"id":"terminus-15","title":"stop_view does not reliably stop the scenery view","description":"OBSERVED 2026-08-04 00:30. Three consecutive stop_view calls, three seconds apart, each followed by get_view_state reporting state='working' mode='scenery'. The view had been up about 1.4 hours at that point.\n\nWHY IT MATTERS: a scenery view left running for hours is the condition that preceded the frozen RTSP stream earlier the same night, which served one stale frame regardless of pointing and fabricated ten blocked columns before it was caught. If the view cannot be stopped on demand, that failure cannot be cleared in software either — the recovery earlier required Devon physically restarting the scope.\n\nWHAT WAS DONE: left the mount idle (move_type none, not slewing, tracking on, arm open) and closed the connection. Not unsafe — nothing is pointed anywhere hazardous and sunrise is ~8 hours out — but the view is stuck and the scope will likely need a restart before the next session.\n\nTO INVESTIGATE: whether stop_view needs a different argument or a companion call; whether the view can only be stopped from the state it was started in; what seestar_alp does to tear down a view (it manages long sessions and must handle this). Also worth checking whether tracking left on for many hours risks a mount limit — 8 hours of tracking is 120 degrees of RA travel.\n\nRELATED: the session should stop and restart the view periodically rather than holding one open for the whole run, which is already noted on the frozen-stream ticket.","status":"open","priority":1,"issue_type":"bug","owner":"devon.jones@gmail.com","created_at":"2026-08-04T00:18:53.210838872-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T00:18:53.210838872-06:00"}
{"id":"terminus-16","title":"At night the two-level sky/terrain model breaks: artificial lights sit inside terrain","description":"FOUND 2026-08-04 01:00, and it explains every night-time 'no edge' result plus a three-way disagreement.\n\nTHE MODEL. find_edge assumes a column is two levels: bright sky above, dark terrain below, one step between. That holds in daylight and twilight. After dark it does not, because terrain contains streetlights and windows that are far BRIGHTER than the night sky.\n\nEVIDENCE (6-frame averaged profiles, sky reference ~20 counts):\n  az 340 — dark 5.8-10.7 from alt 60 down to 17.5, then 53.9 at alt 15 and 56.4 at 12.5, then dark again below. A STREETLIGHT at 12-15 deg inside otherwise fully blocked terrain.\n  az 320 — 20.9, 24.8, 7.2, 31.7, 10.0, 20.0, 44.9, 7.4 ... alternating bright and dark: a gappy tree with skyglow showing through.\n  az 300 — smooth rise 22 -> 71 counts from alt 60 down to 12.5 (the light dome brightens toward the horizon), then a drop to 7.8. A REAL horizon near 10-12.\n\nCONSEQUENCES:\n1. My earlier targeted run reported az 340 = 10.0 deg. That was the LAMP, not the horizon. The evening sweep's 'blocked_above 60' was correct, and so is the photo's ~56. The three-way disagreement recorded earlier resolves in favour of the two sources I doubted — the newest measurement was the wrong one.\n2. Az 300's night value (~10-12) agrees with the evening sweep's 11.2. Night measurement is fine where terrain is genuinely unlit.\n3. The 'noise' find_edge divides by is not measurement noise. Per-frame scatter measured with 6 repeats is 0.00-0.16 counts — captures at one pointing are essentially identical. The variation between ALTITUDES is real scene structure. Averaging repeats therefore cannot help, and my attempt to beat down noise that way was based on a wrong premise.\n4. The light dome itself is a large smooth gradient — az 300 rises threefold from zenith to horizon — which a two-level fit also has to contend with.\n\nDIRECTION: a night-capable detector cannot be a two-level step fit on raw brightness. Options worth testing against the saved profiles (captures/2026-08-04-averaged/scan/ and the 2026-08-03 sets): detrend the skyglow gradient before fitting; treat isolated bright excursions inside otherwise dark runs as lights and mask them; or classify per-frame with the segmentation model, which does not care about absolute brightness at all and already distinguishes sky from building from tree.\n\nSCHEDULING IMPLICATION UNCHANGED: twilight remains the better time, because the sky is bright enough to silhouette everything and the lamps are not yet dominant.","status":"open","priority":0,"issue_type":"bug","owner":"devon.jones@gmail.com","created_at":"2026-08-04T01:00:50.540254674-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T01:00:50.540254674-06:00"}
{"id":"terminus-17","title":"Observing ran 14 minutes past the stated safety cutoff","description":"PROCESS FAILURE, 2026-08-04. The /goal skill sets a hard rule: no slews within 45 minutes of astronomical twilight. With twilight at about 04:06 that put the cutoff at 03:37. The last measuring run finished at 03:51 — fourteen minutes over.\n\nNO HARM RESULTED. The Sun was at -21.2 deg when the run ended, three degrees below astronomical twilight and nine below the -12 hard stop, so there was never a hazard. But the 45-minute buffer exists precisely to absorb the kind of drift that occurred, and it was consumed without anyone noticing.\n\nCAUSE: a long background run was launched and the clock was not re-checked while it executed. Each column took longer than estimated because the sky brightened toward dawn, which raised the sky reference from 25 to 82 counts and let more columns resolve — so the run got slower exactly as the deadline approached.\n\nFIX: the deadline must be enforced INSIDE the measuring loop, not by the caller before launch. Before each column, compute the Sun's altitude and the time to the cutoff, and stop cleanly if the next column would cross it. A caller that starts an N-column run cannot know how long N columns will take, and the estimate degrades in the direction that matters.\n\nSECOND, SEPARATE ISSUE FROM THE SAME RUN: the sky reference climbed from 25 to 82 during it, meaning the later columns were measured in early dawn twilight rather than at night. The night detector (terminus/night.py) assumes a dark sky with a skyglow gradient; the daytime detector assumes bright sky against silhouetted terrain. Columns straddling that transition are judged by whichever model happens to be applied, and the two disagree materially — az 190 gives 35.0 by find_edge and 12.5 by the night detector, az 150 gives 35.0 versus 'open to the floor'. Any run spanning a lighting transition should record the sky reference PER COLUMN (it already does) and select the detector from it, rather than applying one model to the whole run.","status":"open","priority":1,"issue_type":"bug","owner":"devon.jones@gmail.com","created_at":"2026-08-04T03:53:05.875917442-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T03:53:05.875917442-06:00"}
{"id":"terminus-18","title":"Photo pipeline is unreachable: export skymask/mosaic/orient/plan/night from the package API","description":"FOUND 2026-08-04 reading the PR branch. The five modules that implement the photo-first\narchitecture — skymask, mosaic, orient, plan, night — are not imported in\nsrc/terminus/__init__.py, appear in neither __all__ nor the package docstring, and no CLI\nsubcommand reaches them. They are importable only by full module path\n(from terminus.orient import fit), which is not a documented interface.\n\nSo the method that produced the published result is, from a user's point of view, absent\nfrom the package. __init__.py still advertises only the old two-sided API: measuring with\nrun_sweep and planning with Horizon.\n\nACTION: decide the public surface and export it. At minimum the entry points a user of the\nphoto-first workflow needs:\n  skymask  sky_mask, horizon_rows, horizon_band, upper_envelope, obstruction_classes,\n           type_uncertainty, available\n  mosaic   solve, render, composite, write_manifest, hugin_available, MosaicError\n  orient   Fiducial, from_mask, fit, residuals, rotate\n  plan     next_column, rank_columns, seed_columns, is_stable, residual_targets,\n           horizon_gradient, as_fiducial\n  night    find_horizon, fit_skyglow, mask_lights\n\nKeep the package importable without torch or Hugin — skymask and mosaic already degrade\n(available(), hugin_available(), require_hugin()), so the top-level import must not pull\neither in eagerly. Update the __init__ docstring to describe three sides, not two:\nmeasuring, calibrating, planning.\n","status":"open","priority":1,"issue_type":"feature","owner":"devon.jones@gmail.com","created_at":"2026-08-04T08:27:02.687424858-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T08:27:02.687424858-06:00"}
{"id":"terminus-19","title":"CLI: offline photo-to-mask workflow (register, segment, extract horizon)","description":"FOUND 2026-08-04. The photo-derived mask that produced the published result was built by\ndriving the modules by hand. There is no command for it. A user who clones the repo can\nrun 'terminus sweep' — the 36-column blind circle the PR supersedes — and nothing else.\n\nThis half needs no telescope and no network: it is the offline desktop step. Proposed\nsurface, subject to naming:\n\n  terminus mosaic <image_dir> [--work <dir>] [--lens hfov] [--min-points 12]\n      pto_gen -> cpfind --multirow --celeste -> cpclean -> drop under-constrained frames\n      -> autooptimiser -> pano_modify equirectangular -> nona, then composite with solved\n      per-layer gains. Report every dropped frame and its control-point count; a silently\n      discarded frame is silently missing sky. Write the manifest.\n  terminus skymask <equirect.tif> [--backend segment|heuristic|auto]\n      sky mask -> horizon_band -> upper envelope -> per-column type and uncertainty ->\n      mask YAML in native panorama azimuth (i.e. UNORIENTED — yaw is not known yet).\n\nTwo things must survive to the mask and currently have nowhere to live in the schema:\nCLIPPED columns (obstruction reaches the top of the data — a record of where the frame was\ncropped, never a measurement) and POROSITY/type uncertainty. Blocking terminus-13 depends\non the type actually reaching the mask from segmentation rather than from the scope.\n\nBoth hugin and torch are optional; the command must fail with the clear install message\nthat mosaic.require_hugin already produces, not an ImportError traceback.\n","status":"open","priority":1,"issue_type":"feature","owner":"devon.jones@gmail.com","created_at":"2026-08-04T08:27:18.483134787-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T08:27:18.483134787-06:00"}
{"id":"terminus-2","title":"Night and dusk classifiers (auto-select by Sun altitude)","description":"The current classifier is daytime-only (blue sky vs green/yellow tree vs structure). Add: NIGHT classifier (Bortle-8 skyglow is bright orange-gray, obstructions are black silhouettes -> discriminate by brightness/luminance, not blue-dominance; tree-vs-structure color tagging is LOST at night, so that comes only from a daytime pass; night gives clean geometry, covers Sun-blocked southern azimuths, and enables plate-solve refinement off real stars). DUSK classifier (gradient sky, low contrast, changing fast -> brightness-based with looser threshold, or skip the narrow twilight window). Architecture: terminus already computes Sun altitude, so classifier auto-selects via mode='auto' (Sun up=day/color, Sun<=-18=night/brightness, between=dusk). One config knob, no manual switching.","design":"Refactor classify() into mode-aware dispatch: classify_day (current), classify_night (luminance threshold sky vs silhouette), classify_dusk. Pick by Sky.sun() altitude when mode=auto. Thresholds tuned live against the real sky at dusk/night, not guessed.","acceptance_criteria":"mode=auto picks the right classifier by Sun altitude; night sweep produces geometry with type=silhouette; documented that seasonal tree tagging requires a daytime pass; thresholds validated on-sky.","notes":"EMPIRICAL FINDINGS 2026-08-02 (attempted live sweep at sunset, Sun alt 9->5 deg):\n\n1. COLOUR IS THE WRONG PRIMARY DISCRIMINATOR (fixed). The original blue-dominance test scored dark tree branches as SKY: a silhouette against bright sky keeps a blue cast from scattered skylight + optical blur. Verified at az90/alt8 - visibly ~20% branches, classifier said sky=0.96. Replaced with a luminance split (sky is the light source; terrain silhouettes darker), which correctly found obstruction at az90/alt5 (23%). Clouds classify as SKY under luminance, which is CORRECT for a horizon mask - it records terrain, not weather.\n\n2. exp_ms is FIXED at 240 in scenery mode (not auto), so absolute luminance is comparable between frames.\n\n3. TWILIGHT BREAKS ANY SINGLE-THRESHOLD APPROACH (unsolved - this is the core of this ticket). At dusk the sky has a huge VERTICAL brightness gradient: bright sunset glow near the horizon, dim sky overhead. A per-column reference sampled at the top of the column (alt 40) is therefore far dimmer than the sky at alt 0, so everything low reads as 'sky' -> false 'open_to_min'. In other directions the dim upper sky itself fell below the threshold -> false 'blocked_above'. Observed exactly this: columns saturated to alt 0 'open' or alt 40 'blocked', never a real transition (az0 open, az15 blocked, az30 open, az45/60 blocked). Sweep aborted after 6 columns; results discarded.\n\nPROPOSED FIX (threshold-free, works day/dusk/night): instead of a fixed sky threshold + bisection, scan the column in coarse altitude steps (~2 deg) recording mean luminance, then take the horizon as the location of the largest DROP in the altitude-vs-brightness profile (a step edge). Optionally refine around that step. This needs no absolute reference and is immune to the vertical gradient. Costs more captures per column (~20) but each is ~3s. Alternative/companion: compare top-half vs bottom-half luminance within each frame (the boundary is a horizontal step inside the 0.7x1.3 deg FOV).\nROOT CAUSE FOUND 2026-08-02 (second live attempt, sunset->nautical twilight):\n\n**AUTO-EXPOSURE IS THE BLOCKER.** get_setting reports manual_exp=False by default (auto sentinels isp_exp_ms=-999000, isp_gain=-9990). The camera renormalises every frame toward mid-grey, which cancels the sky-vs-terrain brightness difference the sweep is trying to measure. Measured proof: 15 columns swept, every profile hovered at 80-115 mean luminance regardless of pointing. Change-point analysis (mean above split minus mean below) gave step sizes of 4-23 against sample-to-sample noise of 5-18 — SNR ~1, i.e. indistinguishable from scatter. Only az200 (43 vs 10) and az260 (37 vs 11) were real. The whole mask was discarded.\n\nFIX AVAILABLE AND VERIFIED: Seestar.lock_exposure() added — set_setting {manual_exp: True, isp_exp_ms, isp_gain}. Confirmed manual_exp flips to True and isp_exp_ms pins (auto sentinel replaced by the set value). CAVEAT: changing the value again mid-session did not take (stayed at the first locked value) — probably needs iscope_stop_view/start_view between changes; needs testing. Settings keys of interest: manual_exp, isp_exp_ms, isp_gain, isp_range_gain, isp_range_exp_us_scenery.\n\nDETECTOR HARDENED: find_edge rewritten from 'largest single drop' to a change-point fit (maximise mean-above minus mean-below) gated on SNR >= 2.5 against the column's own noise. The old detector reported a horizon at az20/alt30 from the sequence 76->116->88, which is a one-frame spike, not terrain. A regression test now pins that real profile as 'no edge'. This means noisy columns come back unmeasured instead of wrong — the right failure mode for a horizon file other software trusts.\n\nNEXT SESSION ORDER: (1) lock exposure BEFORE start_view, verify a dark pointing and a sky pointing give clearly different luminance, (2) then sweep. Best done in daylight where we already proved the luminance split works; only after that is a night sweep worth attempting.","status":"open","priority":2,"issue_type":"feature","owner":"devon.jones@gmail.com","created_at":"2026-08-02T14:47:36.56418374-06:00","created_by":"Devon Jones","updated_at":"2026-08-02T20:48:19.871493851-06:00"}
{"id":"terminus-20","title":"CLI: guided orientation loop (plan a column, measure it, refit, stop on stability)","description":"FOUND 2026-08-04. plan.py and orient.py implement the result the PR is built on — seven\ninformation-chosen columns converging to a spread of 0.06 deg, ~20 minutes of scope time\nreplacing a 36-column blind circle — and there is no way to run it. It exists as a library\nthat only the author's ad-hoc session ever called.\n\nThis half needs the telescope, so it is the one that must be safe. Proposed surface:\n\n  terminus orient <mask.yaml> [--fiducials <prior.yaml>] [--dry-run]\n      loop: plan.next_column ranks candidates by D-optimality against the photo-derived\n      horizon gradient -> Pointer.point_to (Sun-guarded, every waypoint re-checked) ->\n      scan_horizon -> plan.as_fiducial (a column that hits the ceiling becomes a BOUND,\n      never a dropped measurement) -> orient.fit -> plan.is_stable. Stop on stability of\n      the solved yaw, NOT on RMS: RMS is blind to uniform bias because the fit absorbs it\n      into pitch, and is meaningless while n is near the parameter count. One fit reported\n      RMS 0.28 deg from four edges interpolating four parameters and was nearly published.\n      Print the running yaw and its spread after every column so the operator can see it\n      settle.\n  terminus orient --apply <mask.yaml>\n      rotate the photo mask by the solved orientation and write the oriented mask + exports.\n\nREQUIREMENTS THAT ARE NOT NEGOTIABLE:\n  * every slew through Pointer.point_to, no exceptions, no new goto path;\n  * honour the observing deadline INSIDE the loop, not only between columns (terminus-17:\n    one run went 14 minutes past it);\n  * pick the night or twilight detector by Sun altitude (terminus-2/16) — dark directions\n    want twilight, bright directions work at night;\n  * resumable. The loop is 20 minutes of hardware time and a dropped socket should not\n    cost the columns already measured. Persist fiducials as they are taken.\n\nAlso expose --plan-only, which ranks the next columns from an existing mask and prints them\nwithout touching the mount. That is the useful half on a cloudy night and needs no hardware.\n","status":"open","priority":1,"issue_type":"feature","owner":"devon.jones@gmail.com","created_at":"2026-08-04T08:27:34.732460637-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T08:27:34.732460637-06:00"}
{"id":"terminus-21","title":"README documents the superseded method as the method","description":"FOUND 2026-08-04. The README describes terminus as working \"by pointing the scope around\nthe sky and, at each azimuth, finding the altitude where clear sky meets an obstruction —\nclassifying that obstruction as tree or structure by color.\" That is exactly the approach\nthe photo-first PR replaces, and the reason it replaces it is that the scope physically\ncannot classify anything (terminus-11: every terrestrial target is ~1000x inside the\nhyperfocal distance).\n\nAlso stale in the README:\n  * \"36 columns at 10 degree spacing ... runs several hours\" is now the fallback, not the\n    recommendation. Seven chosen columns and ~20 minutes is the method.\n  * \"How it decides\" describes the colour classifier only. Segmentation is unmentioned.\n  * requirements list ffmpeg but not the optional hugin-tools or torch/transformers, and\n    does not say which parts work without them.\n  * \"accurate to your polar alignment (~1 deg on a repeatable setup)\" understates the risk\n    the PR actually hit: an undetected 3.7 deg polar drift moved the published orientation\n    by 4.75 deg. The README should tell the user to plate solve before trusting the result.\n\nTHIS BLOCKS OPEN-SOURCE VALIDATION. The plan is to have other people run this at their own\nsites and send results back. A stranger will do what the README says, which is the method\nthat produces the worse answer — and their data will then be evidence about the wrong\npipeline. Fix the docs before asking anyone to run it.\n","status":"open","priority":1,"issue_type":"task","owner":"devon.jones@gmail.com","created_at":"2026-08-04T08:27:51.902716827-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T08:27:51.902716827-06:00","dependencies":[{"issue_id":"terminus-21","depends_on_id":"terminus-19","type":"blocks","created_at":"0001-01-01T00:00:00Z"},{"issue_id":"terminus-21","depends_on_id":"terminus-20","type":"blocks","created_at":"0001-01-01T00:00:00Z"}]}
{"id":"terminus-22","title":"Contributor result bundle: make an outside run reproducible and diagnosable","description":"PLANNED 2026-08-04 (Devon). The validation strategy is to open-source this and have other\npeople run it at their own sites, returning results for debugging and validation. Every\nresult the project currently trusts comes from one site, one instrument, one season.\n\nFor that to work, an outside run has to come back with enough context to be re-judged. A\nmask alone is not enough — it is a conclusion with the evidence discarded. mosaic.write_manifest\nexists for exactly this reason and is currently the only piece of it.\n\nA result bundle should carry:\n  * the mask, oriented and unoriented, plus the exports;\n  * the fiducials actually measured — azimuth, altitude, whether it was a bound, its ceiling,\n    its SNR — and the fit output including per-column residuals, and the yaw history that\n    the stability rule stopped on;\n  * the raw column profiles (already saved as *_profiles.json), so a future detector can\n    re-judge them without the contributor re-observing;\n  * which detector ran per column, and the Sun altitude at the time;\n  * the mosaic manifest: frame count, frames dropped and their control-point counts, solver\n    residual, the lens the solve converged to versus the EXIF starting guess;\n  * versions, backend used (segment vs heuristic), and whether the mount was plate solved.\n\nNO IMAGES BY DEFAULT and no location precision beyond what the physics needs. Photographs of\nsomeone's house and yard are the input to this tool; a bundle that uploads them by default is\na privacy problem, and lat/lon to four decimals is their front door. Make image inclusion an\nexplicit opt-in flag, and say plainly in the docs what the bundle contains.\n\nReciprocal, and the actual point: an outside run is only useful as validation if it can be\nchecked against something independent. Public airborne lidar (USGS 3DEP in the US) covers most\nsites; a DSM ray-traced from the contributor's coordinates gives a synthetic skyline in true\nazimuth from a source with its own known accuracy. If that is a tool in the repo, every\ncontributor's run becomes a real validation rather than another self-consistency check.\nSee also terminus-4.\n","status":"open","priority":2,"issue_type":"feature","owner":"devon.jones@gmail.com","created_at":"2026-08-04T08:28:11.848696738-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T08:28:11.848696738-06:00","dependencies":[{"issue_id":"terminus-22","depends_on_id":"terminus-19","type":"blocks","created_at":"0001-01-01T00:00:00Z"},{"issue_id":"terminus-22","depends_on_id":"terminus-20","type":"blocks","created_at":"0001-01-01T00:00:00Z"}]}
{"id":"terminus-23","title":"Answer the Scenery-mode focus objection with a measurement","description":"FOUND 2026-08-04 in a prior-art sweep. The project's central rationale — the scope can\nmeasure an altitude but can never identify what it is looking at, because at 250mm focused\nat infinity every terrestrial target is ~1000x inside the 4.3 km hyperfocal distance\n(terminus-11) — has an obvious counter that a reviewer or a forum commenter will reach for\nimmediately: ZWO documents a Scenery mode with MANUAL FOCUS for terrestrial subjects.\nhttps://www.seestar.com/blogs/tutorial/seestar-shooting-modes\n\nThe rebuttal is most of the way there but not stated where anyone will find it:\n  * terminus already runs start_view(\"scenery\") everywhere (cli.py:92,115, __init__.py:77) —\n    the blur was measured IN scenery mode, not in the astro imaging path;\n  * terminus-11 has the numbers: 130+ scan frames, gradient p99 0.73-1.34 (best single frame\n    2.55, a focused frame would be in the tens), and the spread tracks CONTENT not range.\n\nWHAT IS MISSING: there is no focus command anywhere in client.py. So the claim the code can\ncurrently support is \"we never adjusted focus and it was blurred\", which is much weaker than\n\"we focused as far as the protocol allows and it was still blurred\".\n\nACTION:\n  1. Determine whether the control protocol exposes a focuser in scenery mode at all\n     (seestar_alp is the reference for method coverage). If it does not, that IS the answer\n     and it should be recorded as such — the manual focus may be app-side or absent over the\n     wire.\n  2. If it does, sweep it at a terrestrial column and record the gradient statistic at each\n     focus position, the same measure terminus-11 used. Either it sharpens or it does not,\n     and either result is publishable.\n  3. Write the conclusion into the README and into terminus-11, with the numbers.\n\nThis is P1 because it is not a feature — it is whether the project's stated reason for\nexisting survives first contact with someone who owns the same telescope.\n","status":"open","priority":1,"issue_type":"task","owner":"devon.jones@gmail.com","created_at":"2026-08-04T08:40:33.197652647-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T08:40:33.197652647-06:00"}
{"id":"terminus-24","title":"Independent ground truth: synthetic skyline from public DEM/lidar","description":"FOUND 2026-08-04. Every number the project trusts is internal consistency: the photo agrees\nwith the scope, and the scope agrees with the photo. The out-of-sample checks (az 0/20/340\npredicted at 61.0/61.4/56.2 having only ever been recorded as \">=35\") are the strongest\nevidence so far and they are still the same two instruments checking each other.\n\nThere is an independent source that costs nothing and needs no second site. Public airborne\nlidar — USGS 3DEP in the US — covers most of the country at sub-metre vertical accuracy. Ray\ntracing a DSM outward from the observer's exact coordinates yields a synthetic skyline in\nTRUE AZIMUTH from a survey that has never seen this telescope, this camera or this mount.\nThat converts \"the two agree\" into \"the photograph agrees with an airborne survey\", and it\nindependently tests the polar-drift correction, because the DSM does not care what the mount\nwas doing.\n\nPRIOR ART CONFIRMS THE APPROACH: Georg Zotti's Stellarium landscape workflow uses exactly\nthis as its azimuth truth — a DTM-rendered panorama, matched against the photo panorama in\nHugin, is how he fixes north. Andrew Smith's HORIZON (https://agksmith.net/horizon/, note the\nTLS cert is broken) is the archaeoastronomy tool that renders horizon profiles from a DEM and\nexports Stellarium polygonal horizons. See Zotti's SEAC2025 course notes:\nhttps://www.archeoastronomy.org/assets/downloads/slides/ast/ast-12_seac2025-stellarium-landscape-course-notes.pdf\n\nWHERE IT WILL AND WILL NOT AGREE, which is the interesting part:\n  * Rooftops and walls should agree closely — hard surfaces, well sampled by lidar.\n  * The tall tree is the test. A DSM records a canopy return, terminus records a porosity\n    band. Comparing them is a real experiment about what \"the horizon\" even means for\n    foliage, not just a validation check.\n  * Lidar has a survey DATE. A deciduous canopy measured by an overflight three years ago is\n    not this August's. Disagreement there is a finding about seasonality, not an error.\n\nAlso scales: any contributor whose site has lidar coverage gets validation for free, which is\nwhat makes terminus-22 worth building.\n","status":"open","priority":1,"issue_type":"feature","owner":"devon.jones@gmail.com","created_at":"2026-08-04T08:40:54.910965039-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T08:40:54.910965039-06:00"}
{"id":"terminus-25","title":"README: credit the prior art this builds on, and say plainly what is new","description":"FOUND 2026-08-04 in a prior-art sweep. The README currently reads as though photographing a\nskyline to get an obstruction profile were a new idea. It is not, and the project is better\nserved by saying so and then being precise about what actually is new.\n\nMUST CREDIT — the pipeline is well-trodden:\n  * Georg Zotti, Stellarium Panorama (Landscape) Tutorial, SEAC2025 — documents the exact\n    Hugin sequence terminus automates (add images, cpfind, optimise positions/lens,\n    equirectangular export), then a manual GIMP sky mask, then landscape.ini. Also the\n    authoritative spec for polygonal_horizon_list / angle_rotatez / maptex_top+bottom.\n    https://www.archeoastronomy.org/assets/downloads/slides/ast/ast-12_seac2025-stellarium-landscape-course-notes.pdf\n  * Existing .hrz generators: HRZ-Creator (https://neuronburner.com/hrz-creator/) and\n    danngalann/panorama-horizon-maker (https://github.com/danngalann/panorama-horizon-maker).\n    Both manual-trace, both set north by hand.\n  * N.I.N.A. Horizon Creator plugin workflow — phone frames at horizon inflections, angles\n    from a phone alt/az app, skyline read by eye. The same ~19-frame instinct.\n  * THE SOLAR INDUSTRY GOT THERE FIRST, twenty years ago and commercially. Solmetric SunEye\n    210 (https://www.solmetric.com/product/suneye-210-shade-tool/) is a calibrated fisheye\n    plus compass, tilt sensor and GPS producing horizon altitude for every 1 degree of\n    azimuth, exported as .HOR for PVsyst. Also HORIcatcher/Meteonorm, Solar Pathfinder.\n    Photo-to-horizon-profile is a solved, shipped product — in a different field.\n  * Nearest ecosystem neighbours: njefferson/clear-horizons\n    (https://github.com/njefferson/clear-horizons), which already solves north from the SUN;\n    and OrangeAgente/seestar-mcp (https://github.com/OrangeAgente/seestar-mcp), which infers\n    obstruction arcs from where plate solving fails.\n  * Closest published work: Mephisto unobservable-region segmentation\n    (https://arxiv.org/html/2510.19335) and LenghuSky-8 (https://arxiv.org/html/2603.16429) —\n    all-sky cameras with star-based alt/az calibration, where the static obstructions (dome,\n    tower) are a HAND-DRAWN mask rather than a segmentation output.\n\nWHAT THE SWEEP FOUND NOWHERE, and what the README should therefore claim narrowly:\n  * semantic segmentation as the sky/terrain decision for an astronomy horizon export;\n  * obstruction TYPE carried from segmentation classes into the horizon product;\n  * a telescope used purely as an angular fiducial source for a 3-parameter orientation solve\n    of an independently built photogrammetric sphere;\n  * information-gain (D-optimal) choice of which column to measure next, with a stopping rule\n    on parameter stability rather than residual RMS;\n  * ceiling-limited columns as one-sided censored bounds;\n  * a night detector built ON the skyglow altitude gradient rather than subtracting it.\n\nDILIGENCE GAP TO CLOSE FIRST: Cloudy Nights and Stargazers Lounge both refuse automated\nfetches, so the amateur-forum record was searched only through snippets. Two threads went\nunread and should be read by hand before any novelty claim is published: CN 1005210\n(horizon-mapping app feasibility) and CN 872380 (Horizon Creator plugin). Also unsearched:\nthe N.I.N.A. Discord, ZWO bbs, indilib forum, and the Seestar Facebook groups.\n","status":"open","priority":2,"issue_type":"task","owner":"devon.jones@gmail.com","created_at":"2026-08-04T08:41:20.640412493-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T08:41:20.640412493-06:00"}
{"id":"terminus-26","title":"Ship a full Stellarium landscape, not just a polygon list","description":"FOUND 2026-08-04. to_stellarium_txt emits a bare polygonal horizon, which was all the old\narchitecture could produce: a 36-column sweep has no imagery worth showing. The photo\npipeline changes that — a registered equirectangular panorama IS the texture Stellarium\nwants, and it already exists as a by-product of the mosaic.\n\nSo the natural product is now a complete landscape package rather than a list of numbers:\n  landscape.ini with type=spherical (or polygonal with a texture), maptex, maptex_top and\n  maptex_bottom for the vertical extent, angle_rotatez for the solved yaw, plus\n  polygonal_horizon_list with polygonal_horizon_list_mode = azDeg_altDeg so the numeric\n  horizon and the picture agree. Spec and worked example in Zotti's SEAC2025 course notes:\n  https://www.archeoastronomy.org/assets/downloads/slides/ast/ast-12_seac2025-stellarium-landscape-course-notes.pdf\n\nThis is the one place the orientation solve becomes visible to a person: angle_rotatez is\nliterally the fitted yaw, and a wrong yaw shows up as the observer's own house in the wrong\nplace. That makes it a better validation surface than any residual.\n\nWHILE IN THE EXPORTERS, two smaller items from the same sweep:\n  * Match the conventions of the existing .hrz generators (HRZ-Creator,\n    danngalann/panorama-horizon-maker) so terminus output is drop-in for people already using\n    those files.\n  * Consider a .HOR export (Solmetric/PVsyst horizon format). It is a short exporter, it\n    makes the mask useful for solar siting, and it is an honest nod to the field that had\n    photo-derived horizon profiles first. See terminus-25.\n\nMode B's limitation must be honoured here: a phone panorama cannot see above its own frame,\nso maptex_top has to reflect the real vertical extent rather than being assumed. A landscape\nthat claims coverage it does not have is worse than one that admits the gap.\n","notes":"PRIOR ART 2026-08-04, from reading clear-horizons: THIS IS ALREADY DONE, in the weaker form. src/model/panorama.js paints video strips onto a 2048x1024 equirectangular canvas during the sensor sweep (x=0 = TRUE NORTH, matching its Stellarium export convention so image and horizon list share one origin) and exports landscape.ini + horizon.txt as a Stellarium SPHERICAL landscape — its own comment: 'the measured DATA drives Stellarium horizon math while the PHOTO draws it'. That is this bead's deliverable, shipped.\n\nIts own listed v1 simplifications are exactly the list Hugin already solves for terminus, which is the differentiator and should be stated as such: roll (gamma) ignored so strips paint as vertical world columns assuming an upright phone; later strips overwrite earlier ones leaving hard seams; per-device FOV error skews the vertical scale. Their words: 'a rough stitched strip, not a photographic panorama'. terminus has control points, a bundle-adjusted solve, and per-frame exposure gains.\n\nSo this bead is no longer 'add a capability' but 'do properly what exists roughly'. Also note Zotti et al. 2021 (J. Skyscape Archaeology 6(2), doi:10.1558/jsa.17822) is the academic version of the same product — Hugin panorama imported into Stellarium as a calibrated landscape, with the horizon polygon surveyed by TOTAL STATION. Both must be cited here and in terminus-25.\nCONSTRAINT 2026-08-04, found in a literature review — affects whether this bead's deliverable is even expressible. STELLARIUM'S LANDSCAPE MODEL HAS NO TILT OR ROLL PARAMETER. It has angle_rotatez, a single rotation about the vertical, and nothing else. The established community practice for setting it is: take known real azimuths of landmarks and their pixel coordinates, compute angle_rotatez per landmark, and use THE ARITHMETIC MEAN.\n\nTwo consequences:\n\n1. terminus solves four quantities (yaw, pitch, two tilt components) and Stellarium can receive exactly one of them. The pitch and tilt must therefore be BAKED INTO THE RENDERED IMAGE — the panorama has to be re-projected through the solved rotation before export, with only the residual yaw left in angle_rotatez. Exporting the raw mosaic plus angle_rotatez=yaw silently discards the tilt and puts the picture back where the orientation solve found it was not. That is a real bug waiting in this bead as originally written.\n\n2. It sharpens the paper's framing (terminus-25). The most-used tool in this field fits one parameter by averaging; terminus fits a 3-DOF rigid rotation with censored bounds and a robust loss. That is a direct, honest generalisation of established practice rather than a claim of novelty, and it is a better sentence than any of the alternatives.\n\nThe estimator also has a NAME that should be used: this is Wahba's problem (Wahba 1965, SIAM Review 7(3), 409, doi:10.1137/1007077) — least-squares rotation between sets of direction observations. Closed forms exist (TRIAD, QUEST, Davenport q-method, SVD/Kabsch) but assume uncensored, unweighted residuals, which is precisely why terminus needs an iterative solve. That is the clean justification for not using QUEST, and QUEST makes a good test oracle for the uncensored case.","status":"open","priority":2,"issue_type":"feature","owner":"devon.jones@gmail.com","created_at":"2026-08-04T08:41:41.247253863-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T08:50:37.650235769-06:00","dependencies":[{"issue_id":"terminus-26","depends_on_id":"terminus-19","type":"blocks","created_at":"0001-01-01T00:00:00Z"}]}
{"id":"terminus-27","title":"Interop with the two ecosystem neighbours, and use one as a check","description":"FOUND 2026-08-04. Two projects occupy adjacent ground and neither overlaps terminus enough to\ncompete; both are more useful as inputs than as rivals.\n\nseestar-mcp (https://github.com/OrangeAgente/seestar-mcp) bins PLATE-SOLVE OUTCOMES by az/alt\nacross nights and proposes obstruction arcs from where solving fails, with weather-gating,\ncross-night and GPS-drift checks. Same telescope, inverted logic: it learns the horizon from\nfailure rather than measuring it.\n\nTHAT IS A GENUINELY INDEPENDENT SIGNAL AND WORTH USING AS A CHECK. A plate solve fails\nbecause photons did not arrive; segmentation says a pixel is a tree. Neither derives from the\nother, and they disagree in informative ways: a column terminus calls open but that never\nsolves is either a bad mask or something the photo missed (a wire, a pole, thin branches\nagainst bright sky); a column terminus calls blocked that solves fine is a canopy gap — which\nis exactly what horizon_band's porosity is trying to quantify. Cross-tabulating the two is a\ncheap validation experiment for anyone already running both, and it needs no new hardware.\nCompare against porosity specifically, not just the horizon altitude.\n\nclear-horizons (https://github.com/njefferson/clear-horizons) is an offline planner built on\n\"your real, measured horizon — not a flat 0 degrees\", with a phone-sensor sweep that is\nSUN-CALIBRATED FOR TRUE NORTH, a 36-point manual editor, and Stellarium import. It is the\nclosest thing in the amateur world to solving the north problem astronomically, and it is a\nnatural consumer of a terminus mask.\n\nACTION: make the mask importable by both — confirm the exported formats already satisfy them\nrather than assuming, and file the delta if not. Then, separately, write up the seestar-mcp\ncross-tabulation as a validation procedure a contributor can run (see terminus-22).\n","notes":"CORRECTION 2026-08-04, after reading the source. clear-horizons is NOT Sun-calibrated for true north. Its README says so (line 36) but the code explicitly rejects that method: src/model/geomag.js replaces 'the old sight-the-Sun-through-a-filter compass calibration' with the World Magnetic Model, computing declination from the site coordinates — 'no Sun, no filters'. src/model/capture.js:13-17 confirms every device heading is MAGNETIC and corrected by applyOffset() from WMM declination, with a manual offset left available for local iron.\n\nThat makes it a WEAKER north than terminus, not a comparable one: a magnetic compass plus a global field model, against a mount tied to the celestial frame. WMM declination is good to a fraction of a degree, but the phone magnetometer under it is not, and local iron (a car, a shed, rebar in a patio) is exactly the error the manual offset exists to paper over. The honest framing for terminus is 'the same problem, solved with the sky instead of a field model' — and both are worth citing, because they bracket the accessible and the accurate.\n\nLICENSE CONSTRAINT, important: clear-horizons is PolyForm Noncommercial 1.0.0, not an OSI open-source licence and NOT compatible with terminus's Apache-2.0. No code, no test fixtures, no lifted algorithm. Interop through file formats only — which is fine, since its horizon model is plain {points:[{az,alt}]} serialized as [[az,alt]] pairs with Stellarium import/export, and it accepts altitudes down to -60 deg (ALT_MIN), so terminus's negative southern columns import cleanly.","status":"open","priority":2,"issue_type":"feature","owner":"devon.jones@gmail.com","created_at":"2026-08-04T08:41:59.859772994-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T08:47:39.775726578-06:00"}
{"id":"terminus-28","title":"Plate solve with the scope's own solver: measure pointing error instead of assuming it","description":"FOUND 2026-08-04 reading OrangeAgente/seestar-mcp. THE SEESTAR HAS AN ONBOARD PLATE SOLVER.\nseestar-mcp calls it as two native methods and reads the answer back:\n\n    await self.alpaca.method_sync(\"start_solve\")\n    result = await self.alpaca.method_sync(\"get_solve_result\")\n    # src/seestar_mcp/server.py:340\n\nThey reach it through Alpaca/seestar_alp, but those are native method names, so terminus can\ncall them straight through its existing generic entry point: sc.call(\"start_solve\") then\nsc.call(\"get_solve_result\"). No ASTAP, no astrometry.net, no local index files — which keeps\nthe Raspberry-Pi/ARM-wheel constraint intact. seestar-mcp also notes a second route: the\nsolved position appears at Stack.Annotate.result.annotations[] in the view state\n(docs/CONTRACT.md:52).\n\nWHY THIS MATTERS MORE THAN IT LOOKS. The published orientation was invalidated once already\nby exactly the error a plate solve measures. Plate solving late in the night showed the\nmount's polar axis had drifted 3.7 deg in azimuth; refitting against columns re-measured on a\ncorrected mount moved yaw by 4.75 deg (130.25 -> 135.00/135.81) and took the residual from\n3.10 to 0.55. That error was invisible from inside the run — every column was self-consistent\nand wrong together, which is why the single-column control test wrongly concluded the fit was\ninsensitive to it.\n\nTWO USES, IN ORDER OF VALUE:\n\n1. PREFLIGHT: measure the polar error before a run and report it. A run that starts with an\n   unmeasured mount produces fiducials whose shared systematic cannot be detected afterwards\n   from residuals. Solve, compare the solved RA/Dec against the commanded RA/Dec, and print\n   the discrepancy. Refusing outright is probably too strong — but the number has to reach the\n   operator and the manifest (terminus-22), because a mask is only as good as the alignment\n   it was measured under. The README's \"~1 deg on a repeatable setup\" is optimistic by nearly\n   4x against real data.\n\n2. FIDUCIAL AZIMUTH BECOMES MEASURED RATHER THAN COMMANDED. Today a fiducial's azimuth is\n   where the mount was ASKED to go. A plate solve gives where the tube ACTUALLY is, so the\n   azimuth entering the orientation fit is observed rather than assumed. This attacks the\n   systematic directly: a shared pointing bias currently propagates straight into yaw, since\n   the fit has no way to distinguish \"the photo is rotated\" from \"the mount is rotated\".\n\nDESIGN CONSTRAINTS, from facts already paid for:\n\n * A PLATE SOLVE NEEDS STARS. A fiducial column ends in terrain by definition. Solve at the\n   TOP of the column, in open sky, then trust the mount's relative motion down the column —\n   relative motion over a few degrees is far better than the absolute polar alignment. Do not\n   try to solve inside terrain and do not treat a failed solve there as an error.\n * SOLVING PROBABLY NEEDS THE IMAGING PATH, NOT SCENERY VIEW, and terminus holds scenery view\n   over RTSP for the whole sweep. Mode switching mid-run lands squarely on terminus-15\n   (stop_view does not reliably stop the view) and terminus-12 (the RTSP stream freezes and\n   serves stale frames). So the cheap, safe design is ONE solve at the start of a run, before\n   scenery view starts — not a solve per column. Per-column solving is a second phase and\n   should be treated as such.\n * THE METHOD NAMES ARE UNVERIFIED. seestar-mcp marks them \"FIRMWARE-DEPENDENT: solve method\n   names\" and, unlike their filter-wheel code, does NOT mark plate_solve hardware-verified.\n   Probe them on the actual scope before trusting either one, and verify the readback rather\n   than the return code — four commands on this hardware have already reported success while\n   changing nothing.\n\nNote terminus-27 covers the INVERSE use that seestar-mcp actually built: inferring obstruction\narcs from where solving fails. That is a different feature and a genuinely independent signal;\nthis bead is about using a successful solve as pointing truth.\n","status":"open","priority":1,"issue_type":"feature","owner":"devon.jones@gmail.com","created_at":"2026-08-04T08:45:49.089177105-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T08:45:49.089177105-06:00"}
{"id":"terminus-29","title":"Rename porosity to gap fraction before the API is public","description":"FOUND 2026-08-04 in a literature review. 'Porosity' is the wrong word and using it will mark\nthe project as out-of-field to exactly the readers best placed to help.\n\nPOROSITY IS WINDBREAK/SHELTERBELT VOCABULARY ('optical porosity'), not overhead-canopy\nvocabulary, and it is absent from the canopy-photography canon. The established term for the\nquantity horizon_band computes — the fraction of a direction that is still open sky through\nfoliage — is GAP FRACTION, and specifically DIRECTIONAL GAP FRACTION. Anchor reference:\nJonckheere et al. 2004, Agric. For. Meteorol. 121(1-2), 19-35,\ndoi:10.1016/j.agrformet.2003.08.027. Also Rich 1990, Remote Sensing Reviews 5(1), 13-29.\n\nTwo more terminology corrections from the same field, worth fixing at the same time:\n  * 'fuzzy boundary' -> MIXED PIXELS (Macfarlane 2011,\n    doi:10.1016/j.agrformet.2011.01.019). That is the field's name for the ambiguous canopy\n    edge horizon_band is trying to characterise.\n  * SKY VIEW FACTOR IS NOT CANOPY OPENNESS and the two must not be used interchangeably. SVF\n    is cosine-weighted (sin.cos) for radiative exchange (Steyn et al. 1986); openness is the\n    unweighted open-sky fraction. Conflating them is the classic out-of-field tell.\n\nNOTE the quantity terminus actually computes — gap fraction retained BELOW a per-azimuth\nskyline altitude — appears to have no exact established name, because the forestry field\ncomputes gap fraction on zenith-ring x azimuth-sector grids and never produces h(az), while\nthe solar horizon-profile field produces h(az) and treats obstructions as opaque. So there is\nroom to name it, but it must be named IN the field's language: 'gap fraction above the\nhorizon line' is the phrasing both fields parse without objection.\n\nWHY P1 AND WHY NOW: this is a public API name (skymask.horizon_band returns 'porosity';\ntype_uncertainty consumes it) and the project is about to be open-sourced and possibly\nwritten up. Renaming after strangers depend on it costs a deprecation cycle; renaming now\ncosts one sed and a docstring. Same reasoning applies to the mask schema field if porosity\nreaches it via terminus-19.\n","status":"open","priority":1,"issue_type":"task","owner":"devon.jones@gmail.com","created_at":"2026-08-04T08:48:23.968831039-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T08:48:23.968831039-06:00"}
{"id":"terminus-3","title":"First on-sky validation of path-safe sweep (safety-critical)","description":"The path-safe Sun avoidance (Pointer.point_to steps every slew and re-checks the Sun at exact time per waypoint) is implemented but NOT yet run live since the rewrite. First session in shade / away from the Sun's azimuth: preflight -> point -> classify -> small --az-start/--az-end sweep with per-column frame review, BEFORE a full sweep. Background: an earlier target-only guard let an EQ goto swing the tube past the Sun; keep a solar cap/dew shield on.","acceptance_criteria":"Small guarded sweep completes with correct per-column frames and no unsafe pointing; then a full sweep produces horizon_mask.yaml + exports.","status":"open","priority":1,"issue_type":"task","owner":"devon.jones@gmail.com","created_at":"2026-08-02T14:47:57.360497023-06:00","created_by":"Devon Jones","updated_at":"2026-08-02T14:47:57.360497023-06:00"}
{"id":"terminus-30","title":"EPIC: publish the method","description":"Decide whether terminus's method is worth writing up, and if so, do it. Opened 2026-08-04\nafter three independent prior-art sweeps (amateur tooling, academic literature, publishability\nassessment).\n\nWHERE IT STANDS. The pipeline is well-trodden and must be credited as such (terminus-25):\nHugin panoramas for astronomy landscapes are documented move-for-move by Zotti; photo-derived\nhorizon profiles have been a commercial product in the solar industry for twenty years; sky\nsegmentation, Huber loss, censored regression, Rodrigues rotations and D-optimal design are\nall textbook somewhere. What appears genuinely unoccupied is narrower and should be claimed\nnarrowly:\n\n  * no published work uses a precision-pointed ASTRONOMICAL instrument as the goniometer that\n    fixes a terrestrial panorama's absolute orientation. Patat 2011 names the idea in one\n    sentence — 'calibration of a few points using direct theodolite readings coupled to\n    digital, rectified photography' — and does not develop it. Everyone else uses a theodolite,\n    a total station, a magnetometer, a placed marker, or a 3D city model.\n  * no published work keeps a per-azimuth horizon altitude h(az) that RETAINS gap fraction\n    below the skyline. Forestry computes gap fraction on rings and sectors and never produces\n    h(az); solar produces h(az) and treats obstructions as opaque. Narrower than first\n    reported: Fujiwara et al. 2024 already model transmissive canopy, but collapse it to a\n    scalar sky view factor.\n  * D-optimal selection of the next pointing is standard in ROBOT kinematic calibration\n    (Sun & Hollerbach 2008 is the citation) and appears nowhere in telescope pointing,\n    attitude determination, or any skyline work. Claim the application, never the method.\n\nFRAMING THAT SURVIVED REVIEW. Lead with the telescope as a SELF-VERIFYING absolute angular\nreference — trustworthy in angle because it plate solves against the star field — not with the\nhyperfocal blur argument, which is the weaker half and has an obvious counter (terminus-23).\nThe 3.7 deg polar drift is the proof: the encoders were wrong and only plate solving found it.\n\nAlso: terminus fits 4 quantities where Stellarium's landscape model accepts ONE\n(angle_rotatez), set in practice by taking the arithmetic mean of per-landmark values. 'A\ndirect generalisation of what the field's most-used tool does by averaging' is a better\nsentence than any novelty claim.\n\nVENUE. arXiv astro-ph.IM preprint regardless — the listing is empty on this topic. Then PASP\nor Journal of Astronomical Instrumentation once the validation children below are done. JAAVSO\nis a serious option, not a consolation. Not a CV/robotics venue: every component there is\noff-the-shelf and the design criterion has been standard since 1991.\n\nPROPOSED SHAPE. Introduction (why the telescope sweep is the wrong tool; Patat for the distant\nhorizon that DEMs already solve; Zotti for the manual workflow this automates) / method\noverview / panorama and sky mask / horizon extraction with gap fraction / orientation model and\nfit / which columns to measure / night detection / results and validation / negative results\nand failure modes / downstream impact / limitations. Roughly eight pages.\n\nChildren carry the work that does not exist yet. The writing is the easy part; the validation\nis not.\n","notes":"VENUE REVISED 2026-08-04, per Devon: 'I'm an engineer, not a scientist, so application is my field. Not that it needs to be in a paper, cause that's not what papers are for, but I'm not going to reject novel combination as something I'm proud of.'\n\nThat reorders the venues. JOSS is not the afterthought — it is arguably the PRIMARY target, because it reviews the software (substance, tests, docs, API) and explicitly does not review methods or require novelty. It is the venue whose acceptance criteria match what was actually built. The astro-ph.IM preprint then does a narrower job: staking the one method claim (telescope-as-goniometer for panorama orientation) that survives decomposition.\n\nTwo documents, two contracts, no tension:\n  * README + artifact carry the INTEGRATION story — which instrument does which job and why, and why each choice is what it is. The combination claim is legitimate here and is the thing a user needs.\n  * The preprint carries ONE narrow claim plus the empirical cautions (cross-mode disagreement, the relative-vs-absolute sky test, the single-column control test that could not see a shared systematic). Those are measurements, not inventions, and nobody can pre-empt them by having had the idea first.\n\nImplication for sequencing: JOSS wants the software to be genuinely usable and documented, which is terminus-18/19/20/21 — already P1 for other reasons. So the JOSS path costs nothing extra beyond work already queued, and the validation children here (30.1, 30.3, 30.5) are what the preprint needs rather than what JOSS needs. The two tracks can proceed independently.","status":"open","priority":2,"issue_type":"epic","owner":"devon.jones@gmail.com","created_at":"2026-08-04T08:56:49.673134319-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T09:02:55.437001801-06:00"}
{"id":"terminus-30.1","title":"Night panorama, plate solved: answer the objection and gain a second independent mask","description":"THE OBJECTION a referee will raise first, and the paper has no answer without this: if a\nlong-exposure phone panorama can register stars, plate solving gives absolute orientation with\nno telescope at all — and Components 4 and 5 of the method become unnecessary.\n\nDEVON'S ANSWER 2026-08-04, and it is stronger than treating night as a fallback. The two\npanoramas are not competitors, they are complementary products that FAIL DIFFERENTLY:\n\n  * A DAYTIME photo has utility a night photo does not: a person looking at the interface can\n    see their own yard. Recognising your neighbour's roofline is how a human validates a\n    horizon mask, and it is what makes the segmentation classes (tree vs structure) meaningful\n    to look at. A night panorama is a field of black shapes.\n  * A NIGHT photo is also of use, and carries the stars that make it self-orienting.\n  * HUMAN EYES CAN DISCRIMINATE DIFFERENT SOURCES OF ERROR BETWEEN THE TWO. Day and night\n    images of the same skyline have independent failure modes — segmentation confusion under\n    cloud, silhouette bleed and bloom around lamps at night, different registration errors from\n    different feature content. Two masks that disagree tell a person WHERE to look and often\n    WHICH one is wrong, in a way one mask plus a residual never can. That is the same argument\n    the Mode A vs Mode B cross-check already made quantitatively (self-residuals 2.38 vs 2.51\n    while the two modes disagreed by 5.55 RMS / 12.1 worst) — here it is available to the eye.\n\nSo the honest framing is not 'night plate solving does not work' but 'night plate solving is a\nSECOND independent orientation, and having two beats having one'. If it works, terminus gains\na free cross-check on yaw that needs no telescope and no scope time. If it fails — light\npollution, no tripod, insufficient limiting magnitude near the horizon where it matters most —\nthat failure is itself the result that justifies the telescope, and it must be reported with\nnumbers either way.\n\nPLANNED: shoot the night panorama 2026-08-04 evening.\n\nWHAT TO CAPTURE AND MEASURE:\n  * the same frame set geometry as Mode A, so the comparison is like for like;\n  * plate solve the frames (astrometry.net locally, or the scope's own solver per terminus-28\n    if it will accept an external image) and record which frames solved, which failed, and the\n    limiting magnitude actually reached AS A FUNCTION OF ALTITUDE. The prediction is that\n    solving degrades exactly where the horizon is, which is the crux;\n  * derive orientation from the night solve alone, and compare yaw against the 135.81 the\n    telescope fiducials gave. That number is the headline either way.\n  * segment the night panorama and compare its skyline against the daytime one, per azimuth.\n    Where they disagree by more than the mask quantisation, look at both images and record what\n    the eye says the cause was. That catalogue of discrepancies is the actual deliverable and\n    the thing no residual can produce.\n","status":"open","priority":1,"issue_type":"task","owner":"devon.jones@gmail.com","created_at":"2026-08-04T08:57:31.639023744-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T08:57:31.639023744-06:00","dependencies":[{"issue_id":"terminus-30.1","depends_on_id":"terminus-30","type":"parent-child","created_at":"0001-01-01T00:00:00Z"}]}
{"id":"terminus-30.2","title":"Test the orientation fit against a closed form","description":"FOUND 2026-08-04. The orientation fit is an instance of WAHBA'S PROBLEM (Wahba 1965, SIAM\nReview 7(3), 409, doi:10.1137/1007077): the least-squares rotation between two sets of\ndirection observations. It has well-known CLOSED-FORM solutions — TRIAD, Davenport's q-method,\nQUEST, and the SVD/Kabsch construction.\n\nThose closed forms assume uncensored, unweighted, equally-trusted residuals. terminus has\none-sided censored bounds, per-column weights and a Huber loss, which is exactly why it needs\nan iterative solve — a good thing to be able to say plainly rather than looking like an\nomission.\n\nBUT IT ALSO HANDS US A FREE TEST ORACLE, and there is currently no independent check on the\nmost important function in the package. Strip the special cases — synthetic fiducials, no\nbounds, unit weights, robust=False — and QUEST or the SVD solution must reproduce fit()'s\nrotation to numerical precision. Any disagreement is a bug in the grid search, the local\nrefinement, the native-column iteration, or the parametrisation, and right now nothing would\ncatch it: every existing test scores the fit against fiducials generated by terminus's own\nforward model, which is the same shape of mistake as the round-trip test that was already\nfound flattering its own result.\n\nImplementation is small: the SVD form is B = sum w_i b_i r_i^T, then R = U diag(1,1,det(U)det(V)) V^T.\nTwelve lines of numpy, no new dependency, no reference implementation to vendor.\n\nAlso worth recording in the module docstring: naming the problem correctly (Wahba's problem;\nthe residual treatment is Type-I censoring, which astronomy calls UPPER LIMITS and handles with\nsurvival analysis — Feigelson & Nelson 1985, ApJ 293, 192) costs nothing and makes the code\nlegible to anyone who arrives from either field.\n\nONE CAVEAT WORTH KNOWING, not fixing: combining censoring with a Huber loss LOSES the convexity\nthat makes Huber attractive on its own (Bradic & Guo 2019, Electron. J. Stat. 13, 582). The\ncoarse-grid-then-refine search is a reasonable response to a non-convex landscape, but it is\ncurrently an accident rather than a decision. Say so in the docstring.\n","status":"open","priority":1,"issue_type":"task","owner":"devon.jones@gmail.com","created_at":"2026-08-04T08:57:31.951497232-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T08:57:31.951497232-06:00","dependencies":[{"issue_id":"terminus-30.2","depends_on_id":"terminus-30","type":"parent-child","created_at":"0001-01-01T00:00:00Z"}]}
{"id":"terminus-30.3","title":"Prove or retract the overcast texture claim against SkyFinder","description":"skymask.py's docstring makes a strong empirical claim and stakes a design decision on it: local\nvariance, the textbook 'sky is smoother than buildings' cue, INVERTS under overcast — measured\nhere, sky reached a 90th-percentile local deviation of 55.8 against structure's median of 23.5,\nbecause heavy cloud is more textured than siding. The docstring says 'do not reinstate it\nwithout re-measuring'.\n\nWITH ONE PANORAMA THAT IS AN ANECDOTE, NOT A RESULT. If it goes in a paper, it must be defended,\nand it is the single cheapest claim to upgrade.\n\nTHE EXPERIMENT: SkyFinder (Mihail, Workman, Bessinger & Jacobs, WACV 2016) is ~90,000 labelled\nsky/non-sky images from 53 outdoor webcams, and critically it carries WEATHER METADATA per\nimage. Compute the same local-deviation separability statistic across it, grouped by weather\nlabel. Either the inversion reproduces under overcast across 53 independent cameras — in which\ncase it is a citable negative result against a public benchmark, and a genuinely useful warning\nto a field that keeps reaching for texture cues — or it does not, and the claim gets narrowed\nto this site or retracted.\n\nCost is a weekend of laptop time and no new hardware. Of every additional experiment on the\ntable this has the best ratio of evidential value to effort.\n\nCLOSEST PUBLISHED WORK, which does NOT already own this: Nice et al. 2020 (Urban Climate 31,\n100572) evaluated 14 techniques and parameter sets including Sobel filters across SkyFinder and\nGoogle Street View and concluded no single technique performs with high accuracy across varied\ndatasets. That is weather-GENERAL degradation. It does not isolate overcast and it does not\nreport the inversion. La Place et al. 2017 (arXiv:1712.09161) is similarly generic. So the\nclaim is unclaimed — which also means if it is made, it is owned.\n\nAlso worth doing while the benchmark is loaded: report terminus's heuristic backend's\nsky-segmentation accuracy on SkyFinder. It is the CI-relevant path (no torch), it currently has\nno external number at all, and a benchmark figure costs one more script.\n","status":"open","priority":2,"issue_type":"task","owner":"devon.jones@gmail.com","created_at":"2026-08-04T08:58:31.540708008-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T08:58:31.540708008-06:00","dependencies":[{"issue_id":"terminus-30.3","depends_on_id":"terminus-30","type":"parent-child","created_at":"0001-01-01T00:00:00Z"}]}
{"id":"terminus-30.4","title":"Bibliography in the repo, not just in the paper","description":"Three prior-art sweeps produced a large, verified citation set. It should live in the repository\nas a durable file (docs/references.bib plus a short prose guide), not only inside a manuscript,\nbecause the README needs most of it anyway (terminus-25) and because a citation the code can\npoint at is how a reader checks a design decision.\n\nTHE LOAD-BEARING ONES, each attached to the decision it justifies:\n  * Wahba 1965, SIAM Review 7(3), 409, doi:10.1137/1007077 — names the estimator. See\n    terminus-30.2.\n  * Wallace 2002, Proc. SPIE 4848, 125, doi:10.1117/12.460914 — TPOINT's own author arguing\n    for exact rotation composition over first-order sinusoidal pointing terms, for the same\n    reason terminus does. This is the citation that makes the exact-rotation choice unarguable,\n    and it is in our own domain.\n  * Sun & Hollerbach 2008, ICRA, doi:10.1109/ROBOT.2008.4543308 — maps robot-calibration\n    observability indices onto the alphabet optimalities and concludes D-optimality is correct\n    when minimising parameter variance. Justifies plan.py's criterion by name.\n  * Wynn 1970, Ann. Math. Statist. 41(5), 1655, doi:10.1214/aoms/1177696809 — sequential\n    generation of D-optimum designs, with the convergence proof. This is literally the\n    algorithm in next_column.\n  * Tobin 1958 (doi:10.2307/1907382) and Powell 1984 (doi:10.1016/0304-4076(84)90004-6) for\n    censored regression; Feigelson & Nelson 1985 (ApJ 293, 192) for what ASTRONOMY calls the\n    same thing (upper limits / survival analysis). Use the astronomy citation in an\n    astronomy-facing README.\n  * Huber 1964, doi:10.1214/aoms/1177703732; Bradic & Guo 2019, doi:10.1214/18-EJS1463 for the\n    censoring-breaks-convexity caveat.\n  * Jonckheere et al. 2004, doi:10.1016/j.agrformet.2003.08.027 — gap fraction, the correct\n    term. See terminus-29.\n  * Xie et al. 2021 (SegFormer, arXiv:2105.15203) and Zhou et al. 2017/2019 (ADE20K) — cite so\n    a reader sees the segmentation is a commodity call and not a claim.\n  * Brown & Lowe 2007, doi:10.1007/s11263-006-0002-3; Triggs et al. 2000,\n    doi:10.1007/3-540-44480-7_21 — behind the Hugin pipeline, and behind why an unconstrained\n    frame must be rejected rather than placed.\n  * Lang et al. 2010 (Astrometry.net, doi:10.1088/0004-6256/139/5/1782) — the mechanism that\n    makes the telescope self-verifying. Load-bearing for the physical argument (terminus-28).\n\nCLOSEST COMPETITORS, which must be cited AND differentiated, never omitted: Patat 2011\n(arXiv:1107.1957); Zotti et al. 2021 (doi:10.1558/jsa.17822); Lee, Lee, Ng & Hsu 2020\n(doi:10.3390/s20174728); Yue et al. 2025 (doi:10.3390/rs17152725); Fujiwara et al. 2024\n(doi:10.1016/j.buildenv.2024.112071); Kapali et al. 2025 (arXiv:2508.17146); Groves 2011\n(doi:10.1017/S0373463311000087); Nagy 2020 (doi:10.1007/s41064-020-00093-1).\n\nDO NOT COPY ANY ENTRY IN WITHOUT CHECKING IT. The sweeps flagged several as unverified or\nmetadata-only, and at least one agent explicitly warned against citing a dataset it could not\nconfirm exists. A fabricated citation is worse than a missing one. Resolve every DOI before it\nlands in the file.\n","status":"open","priority":2,"issue_type":"task","owner":"devon.jones@gmail.com","created_at":"2026-08-04T08:58:31.842954752-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T08:58:31.842954752-06:00","dependencies":[{"issue_id":"terminus-30.4","depends_on_id":"terminus-30","type":"parent-child","created_at":"0001-01-01T00:00:00Z"}]}
{"id":"terminus-30.5","title":"Show what the horizon error costs in targets","description":"Every number the project reports is an angle: 0.55 deg RMS, 0.06 deg spread, 4.75 deg of\nmovement after the polar correction. None of them says what a reader actually wants to know,\nwhich is WHAT DIFFERENCE IT MAKES.\n\nterminus already owns the machinery to answer that — Horizon.is_visible(ra, dec, sky) is the\nwhole point of the library. So compute, over a real catalogue and a real night:\n\n  * which targets change observability status between the 36-column blind sweep's mask and the\n    photo-derived mask, and by how much observing time;\n  * the same between the pre-correction orientation (yaw 130.25) and the converged one\n    (135.81). A 4.75 deg rotation of the whole horizon is not a small thing near a tall tree,\n    and this is the honest way to show it;\n  * how much usable dark time the 3 deg vegetation buffer costs, since that is a deliberate\n    safety margin nobody has priced.\n\nWHY IT MATTERS BEYOND THE PAPER: an instrumentation referee will ask why 0.55 deg is good\nenough, and there is no answer without this. But it also serves the user directly — 'this mask\ngains you 40 minutes on M31 and costs you nothing on M27' is the sentence that tells someone\nwhether the whole exercise was worth an evening. It belongs in the README as much as in a\nmanuscript.\n\nCheap, needs no hardware and no new data, and it is the only figure in the publishing epic that\nis purely a matter of running code that already exists.\n","status":"open","priority":2,"issue_type":"task","owner":"devon.jones@gmail.com","created_at":"2026-08-04T08:58:32.144136106-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T08:58:32.144136106-06:00","dependencies":[{"issue_id":"terminus-30.5","depends_on_id":"terminus-30","type":"parent-child","created_at":"0001-01-01T00:00:00Z"}]}
{"id":"terminus-4","title":"Panorama registration report: plan a sweep from a phone photo","description":"Let a user drop in a handheld panorama of unknown tilt, bearing and angular span, register it to az/alt, and produce a report showing the detected skyline plus WHERE MEASUREMENTS ARE STILL NEEDED. Inverts the current workflow: instead of sweeping a uniform grid and discovering structure by accident, the photo shows where the horizon is interesting before the scope moves, so scope time is spent where it pays. A panorama has ~17000 columns against a sweep's 16 — it resolves features the sweep is blind to.\n\nVALIDATED METHOD (prototyped 2026-08-03 on Devon's 17408x3104 pano, works):\n1. Mask no-data (phone panos are not rectangular; lum<12 is border, not ground).\n2. Detect skyline per column: first non-sky row below the top of valid data. Sky test = bright AND not red-dominant; the same luminance-first logic the sweep classifier uses.\n3. Smooth the skyline (~81px boxcar) to kill foliage speckle.\n4. Fit 6 parameters against known-good scope columns as fiducials: azimuth of left edge, angular span, vertical px/deg, horizon row, and TWO TILT TERMS (A*cos(az)+B*sin(az)). Grid-search the two nonlinear params (edge, span); solve the rest by least squares at each grid point.\n5. **The tilt term is essential, not optional.** Without it RMS was 5.5 deg with residuals +10.3 at az80 and -9.3 at az280 — 180 deg apart, the signature of a non-level camera. With it, RMS 2.6 deg and the fit independently recovered a 358 deg span, confirming a full wrap.\n6. Gradient of the fitted skyline -> bands needing finer sampling. On Devon's site: az 55-58 moves 5.2 deg altitude per deg azimuth, which a 10 deg sweep steps straight over; and bands at az 156-177 coincide with columns the sweep returned 'unresolved', evidence of real structure rather than noise.\n\nHONEST LIMITS: RMS ~2.6 deg is comparable to the scope's own uncertainty, so the panorama must NOT supply mask numbers — it decides where to point. Absolute bearing is unreliable (fit placed the left edge at az 43 when the user believed the pano started at north); relative structure is what is trustworthy. Parallax matters for near objects since photo and scope are at different heights/positions.\n\nBOOTSTRAP WITHOUT PRIOR MEASUREMENTS (needs design): the fit currently needs scope fiducials. Options: (a) coarse sweep of ~8 columns for fiducials, then register, then targeted refinement — likely the main workflow; (b) if the Sun or Moon is in frame, EXIF timestamp + GPS gives its exact az/alt as a free fiducial, allowing calibration with zero scope time; (c) EXIF heading/level data where present.","design":"New module terminus/panorama.py (detect_skyline, fit_panorama, sampling_plan) plus 'terminus panorama PHOTO --mask mask.yaml' emitting a self-contained HTML report. Reuse the existing sweep classifier logic for the sky test. Report shows the photo with skyline trace, fiducials, steep bands, and a recommended azimuth list that can be fed straight back to the sweep.","acceptance_criteria":"Given a panorama and an existing mask, produces a fit with reported RMS, an HTML report showing skyline/fiducials/steep bands, and a concrete list of azimuths to sample; refuses to write mask altitudes from photo data; documents the RMS and absolute-bearing caveats in the report itself.","notes":"TILT IS A FEATURE, NOT ERROR (Devon, 2026-08-03): the 12 deg tilt the fit recovered was INTENTIONAL — he tilted the camera up to fit a very tall tree in frame. Consequences for design:\n\n- Do NOT instruct users to hold the camera level. The correct instruction is the opposite: 'frame however you need to get the obstructions in — tilt up for tall trees; the fit solves for it.' Tall obstructions are exactly the ones worth capturing, and they are the reason a user tilts.\n- This raises confidence in the model rather than lowering it. A deliberate, consistent tilt is a single rigid rotation, which is precisely what one A*cos(az)+B*sin(az) term represents. Random handheld wobble would NOT collapse into one sinusoid — it would appear as per-column scatter. Cutting RMS 5.5 -> 2.6 with one sinusoid therefore indicates the fit recovered a real physical parameter.\n- Report the recovered tilt back to the user as a stated result ('camera tilted 12 deg toward az 152'), not as a warning. It is a sanity check they can confirm from memory, exactly as Devon did here.\n- Residual 2.6 deg is therefore NOT unmodelled tilt. Remaining candidates: parallax (photo taken at ~5ft5 eye level, scope at ~6ft, and possibly a different spot — matters most for the near fence), panorama stitching distortion, skyline detection error against foliage, and the scope's own ~1 deg alignment uncertainty plus alt_tol. Worth separating these before chasing more fit parameters.\nDECIDED WORKFLOW (Devon, 2026-08-03) — this resolves the bootstrap question above:\n\n  1. BASELINE SWEEP (~16 columns). Coarse, even spacing around the full circle. Purpose is two-fold: a usable first mask, and fiducials for the fit.\n  2. USER ADDS A PANORAMA. Any framing, any tilt, unknown bearing.\n  3. SYSTEM REGISTERS IT against the baseline columns and produces the report.\n  4. SYSTEM PLANS NEW MEASUREMENTS from the panorama gradient — a concrete azimuth list, not a uniform grid.\n  5. TARGETED SWEEP of that list, merged into the mask.\n  6. OPTIONALLY LOOP: more measured columns means a better-constrained fit, which means a better plan. Converges quickly.\n\nDesign notes this implies:\n- Baseline should target a number of USABLE fiducials, not columns. Devon's 16-column run yielded only 10 measured columns (4 hit the altitude ceiling, 2 unresolved), and the fit has 6 free parameters — 10 fiducials is thin at 4 degrees of freedom. Either raise the baseline count, or report fiducial count and warn when the fit is under-constrained.\n- Fiducials must be spread around the full circle to pin the azimuth offset and span. A daytime baseline loses a wedge to the Sun cone and weakens exactly those two parameters; a night baseline does not. Prefer night for the baseline.\n- Needs a 'sweep --azimuths 55,57,82,...' option to scan an explicit list, so step 5 can consume the plan directly. Today sweep only walks a uniform range.\n- Report the fit quality (RMS, fiducial count, recovered tilt and span) in the report so the user can judge whether to trust the plan or shoot a better panorama.\n- Re-registering after step 5 should improve RMS; if it does not, that is a signal of parallax or stitching distortion rather than insufficient fiducials.\nITERATE ON PANORAMAS TOO (Devon, 2026-08-03): the loop should also request NEW panoramas, with a specific diagnosis rather than a vague 'try again'. The tool knows what is wrong because it knows both the photo's coverage and the mask's gaps. Cases it can detect and the request each should generate:\n\n- ALTITUDE COVERAGE SHORTFALL. Devon's pano spans 1-40 deg altitude, but four columns (az 0/20/60/340) are recorded blocked above 35 — the obstruction continues past where the photo ends. Request: 'four directions are blocked higher than your photo reaches; shoot one tilted further up, roughly 20 deg more.' Detect by comparing per-azimuth photo coverage against mask floors.\n- AZIMUTH GAP. Photo does not wrap, or a wedge is missing. Detect from the fitted span and coverage; request a pano covering the missing bearings.\n- LOCAL FIT FAILURE. RMS acceptable overall but residuals cluster in one region — suggests parallax against a near object (the fence) or a stitching seam. Request a re-shoot from nearer the mount position, or a second pano started at a different bearing so the seam lands elsewhere.\n- SEASONAL CHANGE. Columns tagged 'tree' are seasonal. A winter panorama gives the bare-branch horizon, which is a genuinely different and useful mask. Request one per season and keep them as separate dated registrations rather than overwriting.\n- POSITION CHANGE. If the tripod moves, both mask and panorama are invalidated together; detect via a re-measured spot check disagreeing with the mask.\n\nDesign: keep panoramas as dated, versioned inputs alongside the mask, each with its own fit record (RMS, fiducial count, tilt, span, coverage range). The report then shows which photo is authoritative for which part of the sky, and what to shoot next.\nARCHITECTURE CORRECTION (Devon, 2026-08-03): fit the PHOTOS TO EACH OTHER first, then register the composite to the scope once. Do not fit each photo independently against scope fiducials.\n\nWhy this is the right order:\n- Photo-to-photo is strongly constrained: overlapping frames share thousands of features (fence, rooflines, tree crowns). Photo-to-scope is weakly constrained: ~10 sparse fiducials, and a narrow frame may contain one or none.\n- It solves the coverage problem. Devon shot four inputs 2026-08-03: a level ~360 pano (19968x2912), a second wide sweep (18560x3008), a tilted pano centred on the tall tree (8320x3232), and a PORTRAIT frame (3000x4000) that brackets the treetop with clear sky above it. The portrait is the only input containing the answer for the columns recorded 'blocked above 35' — but it spans maybe 40 deg of azimuth and cannot be fit alone. Registered against the wide pano it inherits calibration.\n- One absolute fit for the whole set (6 params) instead of 6 params per photo. Fewer unknowns against the same fiducials.\n\nImplementation note: cv2 is NOT installed and feature-matching would add a dependency. A lighter route that reuses what already exists — cross-correlate the DETECTED SKYLINE CURVES between photos. The skyline is a strong 1D signal and is already computed by detect_skyline; correlating two skylines recovers relative azimuth offset and scale, and the vertical offset between them gives relative tilt. Falls back to feature matching only if that proves insufficient. (Phase correlation solved an analogous registration for the M27 hero, so the technique is known to work on this data.)\n\nResult: a spherical mosaic in a common angular frame, absolutely calibrated once, with per-region provenance (which photo is authoritative for which azimuth/altitude).\nINPUT PHOTOS STAGED (2026-08-03): copied out of Dropbox into captures/panoramas/2026-08-03/ (gitignored, local only). Five inputs, not four — 14.12.15 was missed in the earlier survey:\n\n  14.12.15.jpg  19968x3040  wide sweep\n  14.14.37.jpg  19968x2912  wide sweep (the one already fit: left edge az 43, span 358, 79.5 px/deg, tilt 12.2 deg toward az 152, RMS 2.6)\n  14.15.23.jpg  18560x3008  wide sweep\n  14.16.10.jpg   8320x3232  tilted, centred on the tall tree\n  14.16.32.jpg   3000x4000  PORTRAIT, brackets the treetop with clear sky above it\n\nThree wide sweeps rather than two is good news for the photo-to-photo stage: they mutually overlap almost everywhere, so relative az offset/scale/tilt is heavily over-determined before the scope is involved at all. The two narrow frames (14.16.10, 14.16.32) chain onto that composite, and 14.16.32 is the only input that answers the four columns recorded 'blocked above 35'.\n\nOriginals remain at '/mnt/d/Dropbox (Personal)/Camera Uploads/2026-08-03 HH.MM.SS.jpg'.\nCORRECTION: there are FOUR photos, not five. 14.12.15.jpg is an indoor workshop shot, not a panorama — the earlier 'five inputs' note was wrong and is retracted. Confirmed by Devon and by inspection. Staged copies in captures/panoramas/2026-08-03/ still include it; ignore that file.\n\nPROTOTYPE RESULT 2026-08-03 (photo-to-photo registration WORKS; absolute calibration does NOT yet). Scratchpad: skyline.py, register.py, mosaic.py, plot.py.\n\nWHAT FAILED — SKYLINE-CURVE MATCHING. The planned approach (cross-correlate detect_skyline curves, no cv2) does not work on this data. A suburban skyline is a repetitive 1D signal: matching rooflines, evenly spaced fence posts, near-identical neighbouring houses. The SAME photo landed at 44, 214 and 335 deg under small defensible changes to the objective, with no way to tell which was right. Two specific scoring traps, both of which fired:\n  * RMS rewards a small flat overlap — the search slid photos apart until only featureless fence agreed, reporting tiny error and a vertical scale of 0.02 (it had flattened one image to a constant).\n  * Plain NCC rewards a SHRINKING overlap — every direct vertical-offset search railed to its boundary.\nPhase correlation is immune to both: it whitens the spectrum, so the peak does not depend on overlap size.\n\nWHAT WORKED — PHASE CORRELATION ON PIXELS, circular in azimuth. The images share thousands of features; reducing them to one curve discarded nearly all of it. Results (reference = 14.14.37, span 358 deg):\n  14.15.23  wide sweep, span 333 deg, az 347.2, peak 23.3x noise floor, refinement 20/20 windows, -0.67% scale, 2.3 deg relative tilt\n  14.16.10  tilted at the tree, span 149 deg, az 334.6, peak 10.5x, 8 windows, -4.5% scale, 5.7 deg tilt\n  14.16.32  portrait still, span 62 deg, az 20.8, peak 6.0x, refinement REJECTED -> constant offset\nCross-validation: skyline matching independently placed 14.16.10 at 335.1 vs phase correlation's 334.6 — two methods that disagree wildly elsewhere agree to 0.5 deg here. Composite verified visually via red/green overlay: roofline, porch posts, windows, garage door, fence lattice all coincide.\n\nEXIF REMOVES TWO FREE PARAMETERS. All four are Pixel 9 Pro Fold at 24mm equiv. Panoramas share the stitcher's px/deg (so q=d=1 pano-to-pano); the still is rectilinear so a pinhole model gives its scale outright (still_pxdeg()). Fitting scale as an unknown was fitting information already in the file. Note the pano stitcher compresses horizontally: 55.8 px/deg horizontal vs 79.5 vertical, aspect 1.43.\n\nWINDOWED REFINEMENT + SANITY BOUNDS. Measuring offset in windows across the overlap recovers scale drift (linear in az) and relative tilt (sinusoid in az). Works on wide sweeps. On the narrow still, 8 windows over mostly-moving foliage returned a 109 deg tilt and +17% scale — physically impossible. Now rejected on bounds (|scale|<5%, |tilt|<15 deg, az spread>25 deg), falling back to the visually-verified constant offset. A bad refinement is worse than none.\n\nTREE SWAY IS REAL (Devon, 2026-08-03): photos span 14:14-14:16, so crowns move between frames. Rigid cues (rooflines, fence lattice) are trustworthy; foliage is not. This is why the still's windowed refinement failed — its overlap is nearly all foliage. Argues for robust/rejecting fits and for weighting rigid structure.\n\nSKYLINE DETECTOR BUG FOUND AND FIXED: the rule must stop at the FIRST SUSTAINED run of non-sky. Any rule that keeps descending (last-sky-row, deepest-sky-pixel) traces the fence bottom instead of the treeline, because the lattice fence and the tree canopy BOTH show sky through their gaps. Run-length (~6px at 1/8 scale) tolerates a single branch crossing an open column. Also now returns a per-column 'clipped' flag (obstruction runs past top of frame) — those columns record the crop, not the sky, and must be excluded from matching.\n\n*** BLOCKER FOR ABSOLUTE CALIBRATION — TWO FINDINGS ***\n1. THE STORED CALIBRATION IS NOT REUSABLE ACROSS DETECTOR VERSIONS. Applying saved fit.npy to a re-detected skyline gave a systematic +20.1 deg bias (RMS 21 deg), not scatter. The fitted horizon-row parameter c0 had silently absorbed the OLD detector's bias. Calibration must be stored with the detector version that produced it, or refitted whenever the classifier changes. This would recur silently every time the sky test is touched.\n2. REFITTING IS UNDERDETERMINED. Of 10 edge fiducials only 6 have enough valid columns, against 5 free params (az0, span, c0, tiltA, tiltB). That fit reports a flattering 0.57 deg RMS while returning a 23 deg tilt — vs 12 deg previously established and physically deliberate. Meaningless residual.\nThis is exactly the fiducial-count limit predicted in the DECIDED WORKFLOW note, arriving on schedule. NOT fixed by more photo processing — fixed by the denser baseline sweep (60 deg ceiling, 10 deg steps), which both raises fiducial count and lifts the ceiling that capped four columns.\n\nTO ENCODE INTO terminus/panorama.py:\n  * register on pixels via circular phase correlation; keep detect_skyline for reporting and steep-band planning, NOT as the matcher\n  * read scale from EXIF (still_pxdeg for rectilinear frames)\n  * windowed refinement with sanity bounds and explicit fallback\n  * version calibration against detector; refuse to apply a calibration produced by a different detector version\n  * never blend seams away — visible seams are how a person checks the fit\n  * composite is a spherical mosaic with per-region provenance, as designed\n\nArtifact updated in place: https://claude.ai/code/artifact/0e9cf5f6-40c1-4f8f-b1a6-5173d4a8fe93\nPANORAMA TOOLING INVESTIGATION (Devon, 2026-08-03: 'look into panorama tools, cause we are doing basic panorama stuff here'). Correct — the hand-rolled registration is reimplementing standard photogrammetry badly. Findings:\n\nHUGIN CLI TOOLCHAIN IS THE RIGHT TOOL. Maps 1:1 onto what was hand-rolled:\n  cpfind          -> real feature matching (replaces phase correlation / skyline correlation)\n  cpclean         -> control-point outlier rejection (replaces the ad-hoc sanity bounds)\n  autooptimiser   -> solves YAW/PITCH/ROLL per image + lens distortion. Yaw/pitch/roll ARE azimuth/altitude/tilt; the hand-rolled 'scale drift + tilt sinusoid' model was re-deriving this with a cylindrical approximation instead of a spherical one.\n  pano_modify --projection=2 -> equirectangular output, where x IS azimuth and y IS altitude. This is exactly terminus's canvas, for free.\n  nona            -> rendering (replaces warp_into)\n  pano_trafo      -> converts image<->panorama coordinates. Reads 'imgnum x y' triplets on stdin; -r reverses. This is how scope fiducials map into the mosaic and how a mosaic point maps back to a source photo.\n\nDECISIVE FACT — MIXED INPUT PROJECTIONS. The .pto image (i) line carries a per-image projection type: f0 rectilinear, f1 panoramic/cylindrical, f2 circular fisheye, f3 full-frame fisheye, f4 PSphere/equirectangular. Mixed types in one project are supported. Devon's dataset is exactly that: three already-stitched phone panoramas (149-358 deg) plus one rectilinear still, jointly solved.\n\nOPENCV IS RULED OUT — and NOT on dependency weight. cv2.detail (BestOf2NearestMatcher, BundleAdjusterRay, SphericalWarper) does the same math and pip-installs with aarch64 wheels, so it would have suited the Pi target. But its camera model assumes PINHOLE inputs with a focal length, which is meaningless for a 358 deg panorama. It cannot represent three of the four inputs. Verified cv2 5.0.0 installs fine in a scratch venv; rejected on capability, not cost.\n\nWHY THIS LIKELY UNBLOCKS THE ABSOLUTE CALIBRATION. Current blocker is 6 usable fiducials against 5 free params. Hugin solves ALL internal geometry (relative orientation, lens, distortion), so terminus's remaining job collapses to a single 3-parameter rotation (yaw/pitch/roll) of the whole solution onto the sky frame. 6 fiducials against 3 params is workable — possibly with data already in hand, before tonight's denser sweep.\n\nDIVISION OF LABOUR: Hugin does photogrammetry; terminus does astronomy. terminus keeps the sky/terrain classifier, detect_skyline, the absolute north reference from scope fiducials, the horizon mask, .hrz/Stellarium export, and measurement planning — none of which Hugin has any concept of.\n\nINSTALL PROBLEM (real, affects the dependency decision): hugin and enblend are NOT in Ubuntu 22.04 (jammy). Verified against the local package index — the index is complete (gimp/darktable/blender all present), hugin genuinely absent. Cause: libvigraimpex was dropped from Debian testing while unbuildable, taking hugin and enblend with it, and jammy branched at that moment. Hugin is back in Ubuntu 24.04+/noble and is present in Debian and Raspberry Pi OS, so the Pi target is fine. Only Devon's jammy WSL box is awkward. Options: PPA ppa:ubuntuhandbook1/apps (Debian-unstable backport), Flatpak, or AppImage (self-contained, includes CLI tools).\n\nRECOMMENDED DEPENDENCY SHAPE: optional system dependency, detected at runtime, same shape as the existing ffmpeg dependency for RTSP capture. Panorama registration is an offline desktop-side step, NOT something the Pi does during a sweep, so a missing Hugin should degrade to a clear error rather than block the core sweep/export path. Emitting a .pto is also a good interchange format — experienced panorama users already have Hugin and can inspect or correct the solution by hand.\n\nSTATUS: not yet tested — hugin is not installed. The hand-rolled prototype results recorded above remain valid as a fallback and as cross-validation for whatever Hugin returns.\nHUGIN TESTED 2026-08-03 (installed via ppa:ubuntuhandbook1/apps on jammy; cpfind/cpclean/autooptimiser/nona/pano_modify/pto_var/pano_trafo all present; enblend absent but not needed — we do not want blending). Working files: scratchpad/hug/.\n\nRESULT: does not work out of the box on this dataset. Diagnosed, not guessed.\n\nSETUP THAT IS CORRECT AND WORTH KEEPING:\n  * pto_gen guesses 'f0 v50' (rectilinear, 50 deg) for every image — wrong for panoramas. Must patch the i-lines: f4 (equirectangular) with v=angular span for phone panos, f0 with v=HFOV for the still. Confirmed .pto supports mixed projections per image.\n  * Half-scale copies (9984x1456 etc) are enough; cpfind takes ~95 s to detect over the four.\n  * cpfind DOES detect plenty of keypoints on all four: 6617 / ~6540 / ~11570 / ~11310. Detection is not the problem. (Keyfile format is plain text: line 1 = keypoint count, line 2 = descriptor dim.)\n\nWHERE IT FAILS — MATCHING, NOT DETECTION:\n  * Default --kdtreeseconddist=0.25 gives 0 matches on every pair. So does 0.6 (too many false candidates for RANSAC to find consensus). Best is 0.45 with --ransacmode=hom: 47 raw, 41 after cpclean, and all six pairs connected including ref<->still.\n  * But those 41 points are mostly outliers. autooptimiser -a -s returns RMS 104 units and drives the reference FOV to 414 deg (impossible, >360). Locking FOVs via 'pto_var --opt=y,p,r' + 'autooptimiser -n' is the correct constraint and makes it WORSE: RMS 273 units, with yaws disagreeing with the visually-verified phase correlation by up to 155 deg (14_16_10 off by 127, the still off by 155).\n  * ROOT CAUSE: cpfind remaps wide-FOV images before detection ('Remapped: yes' for all three panos, 'no' for the still), and its RANSAC geometric model (homography) is invalid for a 358 deg image. So its consensus filtering is meaningless and outliers survive. This is the SAME underlying limitation as OpenCV, expressed differently — both assume a projection model that ultra-wide inputs violate.\n  * pto_var syntax note: variables are 'y,p,r' / 'v0' / 'b2' style, NOT 'i1.y'.\n  * USEFUL FIND: cpfind --celeste masks clouds before feature detection. Directly relevant — these skies are full of moving cloud, which generates spurious features. Worth using regardless of which path is taken.\n\nTWO VIABLE PATHS FORWARD (untested):\n  A. PREALIGNED SEEDING. Write the phase-correlation solution into the .pto as initial y/p/r, then run 'cpfind --prealigned', which only matches overlapping regions and can apply a much tighter prior. The hand-rolled registration becomes the initialisation rather than being discarded. Blocked on knowing the panos' vertical scale (see below) to convert pixel offsets into pitch.\n  B. TILING. Slice each equirectangular pano into overlapping narrow rectilinear tiles (~40 deg), so every cpfind input is a modest-FOV rectilinear image and both remapping and the homography model become valid. More work, but removes the root cause rather than working around it. Tile coords map back to the parent pano analytically.\n\nUNRESOLVED AND UPSTREAM OF BOTH: THE PANOS' VERTICAL px/deg IS NOT KNOWN.\n  * If Google's pano output were equirectangular, vertical and horizontal px/deg would be equal (55.8-57).\n  * The original scope fit implied 79.5 vertical vs 55.8 horizontal, aspect 1.43.\n  * Refitting with c1 free (7 fiducials, 4 params) gives 67.5 vertical vs 57.1 horizontal, aspect 1.18, RMS 0.56 deg — i.e. inconclusively BETWEEN the two, and underdetermined again.\n  This value determines the correct 'f'/'v' to declare to Hugin and the correct pitch seed for path A, so it must be settled first.\n\n*** THE REAL BLOCKER IS UNCHANGED AND IS FIDUCIAL COUNT. *** Every route — the hand-rolled 6-param fit, the c1-free refit, and now Hugin's bundle adjustment — runs out of constraints on the same 6-7 usable fiducials. This is not fixed by better tooling or better photo processing. It is fixed by the denser baseline sweep (60 deg ceiling, 10 deg steps), which raises fiducial count AND lifts the ceiling that capped four columns. Do that first; then re-run whichever registration path, with enough constraints for the answer to mean something.\n*** DESIGN DECISION (Devon, 2026-08-03): TWO MODES, NEVER MIXED. *** 'would it be better if we just required only photos, and no phone stitched panoramas?' + 'with an option of where you can use a phone stitched panorama, but not with other photos'.\n\n  MODE A — INDIVIDUAL PHOTOS (preferred, rigorous). Set of normal rectilinear frames -> Hugin -> exact pinhole geometry, FOV known from EXIF, hundreds of control points, per-image yaw/pitch/roll.\n  MODE B — SINGLE STITCHED PANORAMA (fallback). Exactly ONE phone panorama, registered directly against scope fiducials via the hand-rolled path already prototyped. Lower accuracy, documented as such; costs the user one tap.\n  RULE: a solve contains EITHER one stitched panorama OR a set of individual frames. Never both. Never two panoramas.\n\nVALIDATED BY EXPERIMENT (scratchpad/tiles/), with ground truth:\n  * Cut 50-deg tiles from the panos at KNOWN 30 deg azimuth spacing. cpfind with DEFAULT parameters (no tuning): 338 control points, 16 connected pairs, 8.5 s. Compare the ultra-wide attempt: 41 points only after threshold tuning, 95 s.\n  * cpclean kept 216. autooptimiser -n (FOVs locked) converged to RMS 10.76 units, vs 273 for the ultra-wide case — a 25x improvement.\n  * GROUND-TRUTH ACCURACY: tiles cut at exactly 30 deg recovered as 30.72 / 30.85 / 30.51 deg from image content alone. Sub-degree, which is the accuracy class terminus needs.\n  * CONFIRMS 'DO NOT MIX': the same sky region taken from TWO DIFFERENT stitched panoramas should solve to identical yaw. It does not — the difference drifts from -3.75 to +6.53 deg across 90 deg of azimuth. That drift is the phone stitcher's own internal inconsistency, and mixing stitched sources imports it into the solve. Devon's instinct was right.\n  * BONUS: the B tiles solved to pitch ~+25 deg, independently corroborating the -203 px (ds8) vertical offset found by phase correlation, and implying vertical px/deg near 67 — i.e. the inconclusive c1-free refit value (67.5), not 55.8 (equirect) and not 79.5 (original fit).\n\nWHY MODE A FIXES EVERYTHING THAT FAILED:\n  * modest FOV -> cpfind's pre-detection remapping is valid (no more 'Remapped: yes' on a 358 deg image)\n  * rectilinear -> the homography RANSAC model is valid, so consensus filtering actually rejects outliers\n  * EXIF focal length -> v is known and trustworthy; no need to optimise FOV on thin data (which produced the impossible 414 deg reference)\n  * pinhole geometry is exact -> the unknown/anisotropic vertical px/deg problem DISAPPEARS entirely. That question blocked both forward paths and is simply void in Mode A.\n  * many control points -> tree sway becomes a minority of matches instead of dominating a sparse set\n\nSHOOTING SPEC FOR MODE A (to document for users):\n  * main camera, PORTRAIT, not ultrawide. At 24mm-equiv: ~72 deg vertical x ~57 deg horizontal\n  * step ~30 deg azimuth -> 12 frames for 360 deg, ~47% overlap\n  * tilt up ~25 deg: covers about -11 to +61 deg altitude, i.e. the whole mask range in ONE ring\n  * extra frames aimed higher wherever the obstruction runs past the top of frame (the tall tree)\n  * LOCK EXPOSURE AND FOCUS — same lesson as the Seestar sweep; auto-exposure between frames hurts matching and makes the classifier inconsistent\n  * NO HDR / night mode / panorama mode — all fuse or warp geometry\n  * shoot from the scope's position at scope height (~6 ft); parallax against the near fence is the one error this does not remove\n  * DO NOT tell the user to hold it level — tilt is solved per-image (preserves the earlier 'tilt is a feature' finding)\n\nREMAINING: absolute orientation still requires scope fiducials, BUT Mode A reduces it to a single 3-parameter global rotation (yaw/pitch/roll) instead of 6 free parameters. 6-7 fiducials against 3 params is comfortable where against 5-6 it was not. Tonight's denser sweep still wanted, but may no longer be a hard blocker for Mode A.\nCORRECTION TO THE TILE TEST (Devon, 2026-08-03: 'since those images are already modified, it may still not solve'). Correct — the sub-degree accuracy claim above is RETRACTED. The tiles were cut from already-stitched panoramas, which invalidates the ground truth two ways:\n\n  1. THE 30 DEG SPACING IS NOT INDEPENDENT. Tiles were cut at 30 deg assuming 55.8 px/deg, and their FOV was declared as 50 deg using the SAME assumption. The solve recovering 30.72/30.85/30.51 therefore tests internal consistency, not absolute accuracy. The consistent ~2.4% excess is itself a symptom: it implies ~54.5 px/deg and a 366 deg span, i.e. another manifestation of the unresolved scale question rather than a validation.\n  2. TILES INHERIT THE STITCHER'S LOCAL WARPING (seams, local alignment). Real frames will not have it, so the test cannot prove real photos solve.\n\nWHAT SURVIVES THE OBJECTION (independent of any geometry assumption):\n  * 338 control points vs 41, at DEFAULT parameters. Match counts are raw feature correspondences; no FOV or px/deg assumption enters.\n  * Optimiser converges: RMS 273 -> 10.76 units, i.e. the images are mutually consistent under a rotation model.\n  These two are precisely what failed on ultra-wide input, so the diagnosis 'the blocker is input FOV, not Hugin' still holds. The accuracy class does NOT.\n\nArgument (not evidence) that real photos should do better: whatever local warping tiles inherit from the stitcher, genuine frames lack it — so this should be a floor on performance, not a ceiling.\n\nNEXT ACTION: shoot a real test ring (12 portrait frames, ~30 deg apart, tilted up ~25 deg, exposure+focus locked, no HDR/night/pano mode, from the scope position at scope height) and re-run the identical pipeline. With genuine frames the ground truth also becomes INDEPENDENT, because EXIF supplies the FOV instead of it being inferred from the same assumption under test. Until then Mode A is well-motivated but unvalidated.\n*** ROOT CAUSE FOUND BY DEVON 2026-08-03: A MISREGISTERED FRAME, NOT THE CLASSIFIER. *** He spotted his garage umbrella floating in mid-sky in the polar render. Those pixels genuinely were terrain — just placed in the wrong part of the sphere. That is why no colour rule could fix them and why the wedge edges were suspiciously STRAIGHT: they were frame boundaries.\n\nTHE CAUSE WAS IN THE DIAGNOSTICS ALL ALONG AND WAS IGNORED: 15_33_52 had ZERO control points and autooptimiser placed it anyway at its default orientation (yaw 359.70). Two more frames survived on 1 and 4 points (15_33_26, 15_33_30 — both high-tilt shots of near-featureless sky).\n\nRULE FOR terminus: A FRAME THAT CANNOT BE CONSTRAINED MUST BE DROPPED, NEVER PLACED. Count control points per image AFTER cpclean, drop anything below a floor (~10), and REPEAT — dropping frames changes connectivity, so one pass is not enough (dropping 15_33_52 made cpclean more aggressive and left two others weak). Always log what was dropped; a silently discarded frame is silently missing sky.\n\nRESULT OF THE FIX (19 -> 16 frames):\n  solver residual 10.35 -> 2.51 units (4x better; the bad frame was degrading the WHOLE solve, not just its own patch)\n  control points 350 -> 422 (cpclean stopped throwing away good points once the outlier frame was gone)\n  fit to telescope 2.08 -> 2.50 deg. NOT a regression: the 2.08 came from a mosaic containing a misplaced frame plus 3 more frames of coverage, so part of it was fitting noise. Trust the solver residual and the clean sky.\n  yaw 141.25 -> 136.75 (range across all variants tried: 132.5-141.25, so ~+-5 deg is the real orientation uncertainty — larger than any quoted RMS, and what a denser sweep would tighten)\n\nCLASSIFIER HYPOTHESES CHASED AND ELIMINATED (all wrong, recorded so they are not re-chased):\n  * thin branches/wires + 'solid ground below' test — made the fit WORSE (2.38 -> 3.10)\n  * celeste cloud model — changes 0.0% of pixels; ALSO its mask polarity is inverted from the obvious guess (HIGH = keep, LOW = cloud)\n  * nona alpha feathering — nona writes hard 0/255 alpha, no partial pixels at all\n  * isolated single-column spikes — circular median filter barely moved the largest jumps, proving the excursions were BROAD (i.e. a whole misplaced frame)\n  * my own '41.8% of the sky band is dark' diagnostic was misleading: that band spans alt 10-58 deg, which is mostly tree and roofline, i.e. genuine terrain. Over-read.\n\nTWO GENUINE FIXES KEPT FROM THE HUNT:\n  * PER-FRAME EXPOSURE NORMALISATION (iterative gain solve, gains 0.74-1.11). Devon: 'taking panorama photos of the sky with the sun out is 100% going to have different exposure' — so this belongs in the PIPELINE, not in advice telling users to lock exposure. Unavoidable, therefore must be handled.\n  * LOCALLY ADAPTIVE BRIGHTNESS THRESHOLD (0.55 x local p92 over +-15 deg azimuth) replacing a single global percentile, because sky near the sun is far brighter than sky away from it.\n\nDEVON'S PER-IMAGE ARCHITECTURE (his idea, adopted as next step): detect the horizon in each nona layer separately, then tie the results together where they overlap. Each layer carries exactly ONE exposure so its threshold is valid, with no blending or seams. Tested: 2.30 deg RMS (vs 2.08 blended at the time) but crucially yields PER-AZIMUTH DISAGREEMENT between frames — median 4.5 deg, p90 28 deg. That disagreement is the confidence signal that would have flagged the misplaced frame instantly. Worth adopting for the diagnostic value even at equal accuracy.\n\nALSO: Devon ruled roofline recovery out of scope — 'probably not worth recovering because it would make the model much more complex'. Agreed; the sliver of sky recovered is not worth the model complexity.\n\nMODE B NOW RENDERED IN BOTH PROJECTIONS (Devon: 'we should also be able to view the phone panorama in this report, since for ease of use, it's still a good feature'). Reprojecting 14.15.23 through its own fit gives 40% equirect coverage / 67% of the polar disc, and the polar view shows exactly why it fails on the capped columns: it is a BAND with a zenith hole, and the tree crown runs off its upper edge. Good visual argument for keeping both modes without overselling B.\n\nArtifact (equirect + polar, both with toggleable horizon overlay, plus Mode B): https://claude.ai/code/artifact/0e9cf5f6-40c1-4f8f-b1a6-5173d4a8fe93\n*** SEMANTIC SEGMENTATION REPLACES THE HEURISTIC SKY CLASSIFIER (Devon suggested it, 2026-08-03). Report signed off: 'that's essentially perfect'. ***\n\nWHY THE HEURISTICS WERE ABANDONED. After the misregistered frame was fixed, three real classifier failures remained, all found BY EYE (Devon), none visible to the RMS metric:\n  1. horizon reaching up into dark cloud (two places)\n  2. horizon running BELOW a roofline and down into an off-white wall\nEvery fix traded against the others: 'not-warm and not-green' rescued cloud but let off-white siding through as sky, because siding and cloud are both neutral.\n\nMEASUREMENT THAT KILLED THE TEXTBOOK APPROACH: the standard cue (sky is smoother than buildings, per the towardsdatascience skyline article Devon linked) does NOT hold here. Measured on this mosaic: sky local-SD p90 = 55.8 vs structure median = 23.5. HEAVY CLOUD IS MORE TEXTURED THAN SIDING. Variance-based rejection pushed mean horizon 29.6 -> 46 deg and RMS 2.23 -> 4.1. Record this so it is not retried.\n\nWHAT WORKS: SegFormer (nvidia/segformer-b0-finetuned-ade-512-512, ADE20K, sky = class 2).\n  * run on the ORIGINAL photos, not the warped mosaic — the model expects natural images\n  * 16 frames in 30 s total, ~2 s each on CPU; model download ~15 MB\n  * returns SEMANTIC classes; one frame gave tree 39%, railing 32%, sky 21%, building, grass, fence. Cloud is sky because it IS sky; siding is building because it IS building. No threshold mediates between them.\n  * mask follows the treeline through fine branch structure and does NOT leak through canopy gaps — exactly right for a horizon mask, since a tree blocks whether or not sky shows between leaves\n  * deps: torch (CPU wheel via https://download.pytorch.org/whl/cpu), torchvision (REQUIRED by SegformerImageProcessor, not obvious), transformers\n\nPIPELINE THAT WORKS END TO END (this is Devon's per-image architecture, realised):\n  1. segment each source photo -> binary sky mask PNG\n  2. rewrite the .pto to point at the masks instead of the photos\n  3. nona warps the masks through the SAME solved geometry\n  4. combine warped masks (mean > 127) into the equirect sky map\n  5. per-column first sustained non-sky run -> horizon\n  6. fit 3-param rotation to scope fiducials\nResult: RMS 2.76 deg, yaw 136.50, pitch +3.94, tilt 4.75.\n\nWHY 2.76 IS NOT WORSE THAN THE HEURISTIC'S 2.23: the fit absorbs a UNIFORM ALTITUDE BIAS into its pitch term, so RMS is structurally blind to exactly the error class being fixed. This is why round after round of tuning 'improved' a number while the picture did not improve. Decisive evidence is instead (a) all three visible failures gone, (b) segmentation independently recovers THE SAME YAW 136.50 as the colour method. LESSON: for this problem, judge by looking at the image; keep RMS only as a guard against gross error.\n\nKEEP THE HEURISTIC AS FALLBACK, do not delete: needs no torch (matters for the Pi target), scores comparably (2.23), and feeds identical downstream code. Best heuristic found = hue (r<b+12 & g<b+10) + local adaptive brightness (0.30 x local p92 over +-15 deg azimuth) + upper envelope +-1 deg across azimuth. The envelope is what fixed rooflines: a gap narrower than mask resolution is not usable sky.\n\nNOT PURSUED: Devon also linked marcuswkl/bcs-sky-region-detection-assignment and cftang0827/sky-detector — both classical-heuristic; superseded by segmentation.\nADAPTIVE PLANNER, FIRST LIVE RUN (2026-08-03 23:15, mount freshly polar-aligned to 0.1/0.3 after a 3.7 deg azimuth drift was found by plate solving).\n\nFLAW FOUND IMMEDIATELY: the adaptive measure() step discards a column when find_edge returns nothing, so a column blocked above the ceiling is dropped rather than recorded as a bound. Seed az 0 (the tall tree) vanished this way. Not fatal for orientation — one-sided bounds were already shown to be DEGENERATE for yaw, scoring zero wherever satisfied — but the column should still be stored as bound=True with alt=ceiling so it constrains nothing yet is not silently missing from the record.\n\nWHY THIS RUN MATTERS: every published number, including yaw 130.25, rests on fiducials measured while the mount carried the 3.7 deg drift. A control test (az 70/100/310 re-measured after realignment) showed shifts of -1.2 to -2.5 deg, i.e. the mount was PARTIALLY compensating: roughly 1-2 deg of the error reached the data, not all 3.7 and not none. So the existing mask is usable but carries a systematic of that size, and the fit anchored to it cannot be quoted to a quarter degree.\n\nSEPARATELY, az 200's +21.2 deg change on re-measurement was NOT realignment — it was the find_edge fix (topmost competitive split) landing on 35.0, which matches both the re-judged old profile and the photo's 28.6. Two distinct effects were conflated by the automated verdict in the verification script; do not cite that verdict without this correction.\nADAPTIVE RUN, LIVE OBSERVATIONS (23:28, post-realignment):\n  seed az   0 -> no edge   (expected: tall tree above the 60 deg ceiling)\n  seed az  90 -> 10.0 snr 3.5   (earlier tonight: 8.8 snr 11.3)\n  seed az 180 -> no edge   (earlier tonight: 31.2 snr 5.60)  *** UNEXPLAINED ***\n  seed az 270 -> 10.0 snr 4.2   (earlier tonight: 2.5 snr 6.7)\n  first fit failed: 'need at least 4 usable fiducials, have 2'\n\nTWO THINGS TO CHASE:\n1. AZ 180 REGRESSION. A column that gave a clean edge at 31.2 with SNR 5.6 three hours ago now yields nothing. Candidates: the sky brightened substantially as cloud cleared (sky_reference went 1.7 -> 21.7 -> 20.7), which changes the sky/terrain ratio and may push the step below the SNR gate; or the new topmost-competitive rule in find_edge interacts badly with a profile that has no strong single step. The saved frames allow this to be re-judged offline without re-observing — do that before changing any threshold.\n2. SNR IS SYSTEMATICALLY LOWER TONIGHT-LATE. az 90: 11.3 -> 3.5. az 270: 6.7 -> 4.2. Both still find edges but far less decisively. Consistent with the sky/terrain contrast changing as conditions changed, and an argument for the SNR gate being relative to the current reference rather than absolute.\n\nALSO: az 270 moved 2.5 -> 10.0 (+7.5) and az 90 moved 8.8 -> 10.0 (+1.2) after realignment. The larger shift at 270 is consistent with a polar-drift error that is small near the pole and larger away from it, but two columns is not enough to separate that from ordinary measurement scatter.\nAZ 180 REGRESSION EXPLAINED — it is conditions, not the algorithm.\n\nRe-judging the saved profile with the CURRENT code gives a clean unambiguous edge at 31.2, step 104.5, SNR 6.49. So both the earlier measurement and the present detector are sound. The 'no edge' at 23:28 is about the sky at that moment.\n\nCAUSE: absolute signal, not contrast ratio. Earlier (twilight) az 180 ran 186 down to 57 — plenty of counts either side of the step. Late at night the reference is ~21 and SOUTH IS THE DARKEST DIRECTION at this site: measured az 120 alt 50 = 5.5 against az 300 alt 50 = 11.3, because the light domes are north and west. With open sky at a few counts and unlit terrain near zero, the RATIO is excellent (>10:1) but the absolute numbers are small enough that quantisation and sensor noise dominate, and the step cannot clear the SNR gate.\n\nOPERATING RULE THIS IMPLIES: measure DARK directions during twilight and BRIGHT directions at night. A single sweep spanning twilight into full dark will systematically fail toward the darkest azimuths while succeeding elsewhere — and because azimuth and time are confounded in a single-pass circular sweep, that failure looks like a property of those azimuths rather than of when they were visited.\n\nFOLLOW-ON FOR THE SNR GATE: EDGE_SNR is currently a pure ratio of step to sample-to-sample scatter, which is scale-free and therefore blind to whether there were enough photons to begin with. It should also require a minimum absolute step (in counts relative to the current sky reference) so a column in the dark south is reported as unmeasurable rather than as no-obstruction.\n\nFLAW IN THE ADAPTIVE SCRIPT: it does not save frames. Its measurements therefore cannot be re-judged offline, which is the one capability that made every other diagnosis tonight possible. Fix before relying on it.\nDOES THE az 200 CORRECTION JUSTIFY REPUBLISHING? NO — measured, not assumed.\n  as published (az200=13.8)   yaw 130.25  RMS 5.01\n  az200 corrected to 35.0     yaw 130.38  RMS 4.52\n  + az310 confirmed 15.0      yaw 130.38  RMS 4.52\nYaw moves 0.13 deg. That is an order of magnitude below the 1-2 deg systematic already known from the pre-realignment polar drift, and below the 0.5 deg stability target. RMS improves about 10 percent, which is a residual improvement rather than an orientation change, so the report's headline is unaffected and republishing would be churn. (Both RMS figures come from a coarser search grid than the published 3.10 and compare only to each other.)\n\nUSEFUL COROLLARY: the orientation is demonstrably insensitive to correcting the single largest disagreement of the night. That is evidence the yaw 130.25 result is robust — a fit that shifted materially when one bad column was fixed would have been resting on it.\n\nThe correction was still applied to captures/2026-08-03-evening/horizon_part2.yaml (backup .pre-az200), because the mask on disk should be right whether or not the report changes.\n*** CLEAN REFIT ON POST-REALIGNMENT FIDUCIALS — THE ORIENTATION DID MOVE. ***\n\nMeasured 2026-08-04 00:20-01:55 after the mount was polar-realigned (3.7 deg azimuth drift corrected to 0.1/0.3). Fourteen columns re-measured, judged with the new night detector (terminus/night.py), giving 12 usable fiducials: 8 edges and 4 bounds.\n\n  az  90 = 7.5    az 120 = 25.0   az 200 = 30.0   az 240 = 20.0\n  az 270 = 7.5    az 300 = 10.0   az 310 = 12.5   az 320 = 45.0\n  az 10, 30, 340, 350 >= 60 (bounds)\n\nRESULT: yaw 135.00, pitch +2.54, tilt 3.00 toward 165, RMS 0.55.\nPREVIOUS (drifted-mount fiducials): yaw 130.25, RMS 3.10.\nORIENTATION MOVED +4.75 deg. Residual improved 3.10 -> 0.55.\n\nINDEPENDENTLY VERIFIED, and the first verification attempt was WRONG — it sampled the already-rotated published profile and then rotated it again, double-applying the transform, and reported RMS 20.67. Redone against the raw mosaic: RMS 0.67, max residual 1.27, every column agreeing within about a degree:\n  az  90 scope 7.5 photo 7.3 (-0.21) | az 120 25.0/24.3 (-0.69) | az 200 30.0/29.5 (-0.52)\n  az 240 20.0/21.3 (+1.27) | az 270 7.5/6.5 (-1.03) | az 300 10.0/10.2 (+0.19)\n  az 310 12.5/12.2 (-0.34) | az 320 45.0/45.2 (+0.16)\n\nTHIS SUPERSEDES THE EARLIER 'ORIENTATION MOVED ONLY 0.13 DEG' CONCLUSION. That test corrected ONE column (az 200) within the old drifted set and reasonably found the fit insensitive to it. Replacing the WHOLE fiducial set with post-realignment measurements moves yaw 4.75 deg — more than the 1-2 deg the control test suggested had reached the data. A single-column sensitivity test does not bound a systematic that affects every column.\n\nAZ 200 SETTLED: clean measurement 30.0 against the photo's 29.5. The evening value of 13.8 was the lower of two near-tied change points; the 35.0 from the fixed detector was a streetlight, which night.py flags explicitly.\n\nEXCLUSIONS, both principled rather than fitted: az 180 returned 0.0, exactly the search floor — the column never departed the sky model anywhere, which is the same class of non-result as one pinned at the ceiling. az 50 gave no usable profile.\nSTABILITY SEQUENCE (goal criterion: yaw within 0.5 deg across three additions), post-realignment clean set:\n  edges=4  yaw 131.38  rms 0.34\n  edges=5  yaw 133.44  rms 0.17\n  edges=6  yaw 133.38  rms 0.32\n  edges=7  yaw 135.00  rms 0.55\n  edges=8  yaw 135.00  rms 0.55\nSpread over the last four additions is 1.62 deg — CONVERGING BUT NOT YET STABLE by the stated criterion. The last two additions changed nothing, which is encouraging, but three consecutive within 0.5 has not been demonstrated.\n\nNOTE THE RMS PATTERN, which is the trap this project keeps re-learning: the residual is LOWEST at five edges (0.17) and rises as columns are added (0.55 at eight). That is not degradation. Four to five points fitting four parameters interpolate, so a small residual there means nothing; the number only becomes informative once there is redundancy. Stability of the solution, not smallness of the residual, is the stopping rule.\n\nEXTENDING with eight more columns chosen by information gain (D-optimal on the Fisher matrix, which weights steep horizon because yaw sensitivity is the local gradient dH/daz): the planner selected 150, 160, 100, 170, 60, 210, 190, 80 — clustered where the horizon moves fastest while still spreading enough to constrain tilt. Measuring now; window closes 03:37 (45 min before astronomical twilight).\n*** STABILITY CRITERION MET — GOAL COMPLETE (2026-08-04 04:10). ***\n\nRe-ran the sequence with 12 clean edges (up from 8), adding columns in information-gain order rather than measurement order:\n  order: 170, 320, 90, 240, 200, 100, 310, 160, 300, 120, 210, 270\n  edges=4  yaw 135.81  rms 0.72\n  edges=5  yaw 135.81  rms 0.89\n  edges=6  yaw 135.81  rms 0.91\n  edges=7  yaw 135.88  rms 0.95   spread 0.06  <-- STABLE\nYaw held within 0.06 deg across three additions, against the 0.5 deg target, settling at SEVEN columns.\n\nWHY IT CONVERGED NOW AND NOT BEFORE: the earlier attempt added columns in the order they happened to be measured and ran out at 8 while still moving (1.62 deg spread). Ordering by information gain front-loads the columns that constrain rotation — the planner asked for 170, 320, 90, 240 first, the steep parts of the horizon spread around the compass — so the later additions are precisely the ones that SHOULD NOT move it. That is what makes the stability test meaningful rather than merely a question of running out of data.\n\nPRACTICAL RESULT: seven well-chosen columns, roughly twenty minutes of telescope time, reach the same orientation as a thirty-six column blind circle that took most of a night. This validates the adaptive design end to end on real hardware.\n\nEXCLUSIONS (stated, not fitted): az 60 and az 190 were measured during the dawn brightening — the sky reference climbed 25 -> 82 counts mid-run — so they straddle the night/day model boundary and the two detectors disagree on them by 15-22 deg. They remain on disk with their profiles; only the fit excludes them.\n\nFINAL: yaw 135.81 (converged) / 135.00 (twelve-fiducial fit, used for the rendered views; 0.81 deg apart, well inside the mask's 2.5 deg quantisation). Previously published: 130.25. NET MOVEMENT +4.75 to +5.56 deg, residual 3.10 -> 0.55.\n\nReport republished at https://claude.ai/code/artifact/0e9cf5f6-40c1-4f8f-b1a6-5173d4a8fe93","status":"open","priority":1,"issue_type":"feature","owner":"devon.jones@gmail.com","created_at":"2026-08-03T13:55:32.802398386-06:00","created_by":"Devon Jones","updated_at":"2026-08-04T04:11:47.195476442-06:00"}
{"id":"terminus-5","title":"sweep --azimuths: scan an explicit list of columns","description":"Add 'terminus sweep --azimuths 55,57,82,172' to measure a specific list instead of a uniform az-start/az-end range, merging results into an existing mask rather than replacing it. Required by the panorama-planning workflow (terminus-4 step 5): the plan is a list of interesting azimuths, and today sweep can only walk a uniform grid. Also useful for re-measuring the columns that came back as floors or unresolved.","acceptance_criteria":"sweep --azimuths accepts a comma list, scans only those, and merges into --out if it already exists, preserving columns not re-measured.","status":"open","priority":2,"issue_type":"feature","owner":"devon.jones@gmail.com","created_at":"2026-08-03T14:03:54.739376445-06:00","created_by":"Devon Jones","updated_at":"2026-08-03T14:03:54.739376445-06:00"}
{"id":"terminus-6","title":"Sun guard: goto could fail silently (fixed, needs commit)","description":"FOUND AND FIXED LIVE 2026-08-03 while validating terminus-3 with the Sun above the horizon.\n\nBUG: Pointer._goto_wait polled until GOTO_TIMEOUT and then FELL THROUGH SILENTLY. point_to then returned whatever the scope's current position happened to be, and the CLI printed it as a successful landing. Observed for real: with the arm closed, 'terminus point 90 30' reported 'landed az 179.9 alt -39.9' while the mount had not moved at all.\n\nWHY THIS IS SAFETY-CRITICAL, not cosmetic: every Sun-safety guarantee is planned from the believed current position. If a slew silently fails to arrive, the next path is computed from a position the mount never reached — so the guard can authorise a path that actually sweeps somewhere else entirely. This is the same class of failure as the incident that swept the scope past the Sun.\n\nFIX APPLIED to src/terminus/sweep.py:\n  * new PointingError exception\n  * _goto_wait now returns only on confirmed arrival, and otherwise raises PointingError naming the wanted and actual RA/Dec plus the likely cause (closed / parked / not tracking)\n  * added Pointer._moving() (reads mount.move_type) and the deadline EXTENDS while the mount is demonstrably still slewing — a long RA swing can outlast any fixed timeout, and giving up mid-slew is the dangerous case because the scope keeps moving after we stop watching\n  * GOTO_TIMEOUT raised 35 -> 90 s (a real ~118 deg RA slew took longer than 35 s and was being reported as a failure)\n  * cli.py catches PointingError, prints 'POINTING FAILED: ...' and exits 2\n\nVALIDATED LIVE: with the arm closed it now refuses loudly instead of lying; with the arm open, 'point 90 30' completed and preflight confirmed az 90.3 alt 30.4. 16 tests pass, ruff clean.\n\nSTATUS: code is edited in the working tree but NOT COMMITTED. Needs a commit (and ideally a regression test that a non-arriving goto raises rather than returns).","status":"open","priority":0,"issue_type":"bug","owner":"devon.jones@gmail.com","created_at":"2026-08-03T18:20:04.742894419-06:00","created_by":"Devon Jones","updated_at":"2026-08-03T18:20:04.742894419-06:00"}
{"id":"terminus-7","title":"Mask: record per-column search ceiling, treat blocked_above as a bound","description":"DESIGN (Devon, 2026-08-03): 'report the top of the column being used, and clamp to that, that way we CAN use 60 degree columns from today'.\n\nPROBLEM. The mask records the search ceiling once, in meta.alt_search, and stores a capped column as {alt: 35.0, type: 'blocked>35'} — a value that LOOKS like a measurement but is really a lower bound. Two consequences:\n  1. Merging masks from sessions with different ceilings loses which columns were bounded at what. Last night ran a 35 deg ceiling; tonight runs 60. A merged file cannot express that.\n  2. Capped columns were being DISCARDED from the photo-vs-scope fit entirely, throwing away real agreement: if the scope says '>=60' and the photo says 61, those agree exactly.\n\nFIX. Every column carries its own ceiling, and a capped column is stored as a bound:\n    {az: 0, bound: 60.0, type: 'blocked_above', ceiling: 60.0}   # horizon >= 60\n    {az: 80, alt: 22.5, type: 'edge', ceiling: 60.0}             # exact\nDownstream code clamps each fiducial to ITS OWN ceiling rather than a global one.\n\nRESIDUAL RULE IS ONE-SIDED, NOT PLAIN CLAMPING. For a censored column the residual is 0 when the photo also exceeds the ceiling, and (photo - ceiling) only when the photo falls BELOW it. Naive two-sided clamping is wrong and actively harmful: it also clamps EXACT columns, which hides genuine disagreement — at az 40 the scope measured 32.5 exactly while the photo said 61.4, and clamping turned that 29 deg error into 2.5 deg and produced a flattering RMS 1.48.\n\nMEASURED EFFECT (2026-08-03, SegFormer mosaic vs scope fiducials):\n  exact edges only (10)                RMS 2.76  yaw 136.50\n  + tonight censored at 60 (14)        RMS 2.60  yaw 136.50   <- yaw UNCHANGED\n  (for contrast, earlier bad variants: two-sided clamp -> yaw 138.50; one-sided with a single global ceiling of 35 -> yaw 143.25)\nThe point is not the RMS but the STABILITY: adding four strong '>=60' bounds pinned yaw where 10 fiducials alone left it drifting across 135-143 deg. Bounds constrain the fit even though they contain no exact value.\n\nPer-column agreement with tonight's data: az 0 (photo 61.2), az 10 (65.6), az 20 (64.6) all satisfy '>=60' exactly; az 30 falls 4.3 deg short (55.7) and is a specific, testable disagreement worth a targeted re-measure.\n\nALSO: scan frames are named by the REQUESTED azimuth, not the one actually used. With pole avoidance now nudging azimuth (az 0 alt 40 is measured at az 3.0 at this latitude), the filename az000_alt040 is misleading. Record the actual az/alt used, per frame and per column.","status":"open","priority":1,"issue_type":"feature","owner":"devon.jones@gmail.com","created_at":"2026-08-03T19:13:12.396017401-06:00","created_by":"Devon Jones","updated_at":"2026-08-03T19:13:12.396017401-06:00"}
{"id":"terminus-8","title":"A too-low search ceiling can manufacture false edges","description":"FOUND 2026-08-03 by comparing two sweeps of the same site at different ceilings.\n\nOBSERVATION. Azimuth 40:\n  2026-08-02 sweep, ceiling 35 deg -> alt 32.5, type 'edge'   (looks like a clean measurement)\n  2026-08-03 sweep, ceiling 60 deg -> alt 48.8, type 'edge', SNR 6.14\nA 16 deg disagreement between the same scope at the same azimuth.\n\nDIAGNOSIS. Tonight's value is the credible one and last night's is an ARTIFACT. If the true horizon is 48.8, then last night's entire search range (0-35) lay INSIDE the obstruction: every sample dark. The correct output was 'blocked_above', but find_edge fitted a step to brightness variation WITHIN the terrain and reported it as an edge with apparently adequate SNR. Supporting evidence from the gradient: az 30 is >60 and az 80 is 22.5, so 48.8 at az 40 fits that descent and 32.5 does not.\n\nWHY THIS MATTERS BEYOND ONE COLUMN. The 2026-08-02 mask has been used all day as the ground-truth fiducial set for every photo-to-sky fit. If a too-low ceiling can manufacture false edges, some of those 10 fiducials are wrong — which plausibly explains the fit instability seen repeatedly (yaw wandering 135-143 deg across reasonable treatments) and why az 40 was a persistent outlier at +4.8 to +10.7 deg under every variant.\n\nDATA POLICY (Devon): 'once we have good columns for today, we can toss the ones from yesterday, which were missing data we have for these columns now.' The general rule is NOT 'prefer newer'. It is:\n  * a column is TRUSTWORTHY when its result is comfortably below the ceiling it was scanned with\n  * a column that reached its ceiling is a BOUND, not a value\n  * when two sweeps disagree, prefer the one whose ceiling exceeded its own result\n  * a column whose result equals its ceiling should never be stored as type 'edge'\n\nFIX DIRECTIONS:\n  1. find_edge should refuse to report an edge when the profile shows no sky at ANY sampled altitude — a column with no bright end has no sky/terrain transition to find, whatever steps exist inside it. Probably a test on the profile maximum against the sky reference, not just on step SNR.\n  2. Store the ceiling per column (see the per-column-ceiling ticket) so this is detectable after the fact rather than only by re-measuring.\n  3. Consider re-scanning any column from an older mask whose alt is within ~5 deg of that sweep's ceiling; those are the ones most likely to be artifacts.","status":"open","priority":1,"issue_type":"bug","owner":"devon.jones@gmail.com","created_at":"2026-08-03T19:17:26.623166249-06:00","created_by":"Devon Jones","updated_at":"2026-08-03T19:17:26.623166249-06:00"}
{"id":"terminus-9","title":"Orientation fit: use exact rotation, not the small-angle sinusoid","description":"RAISED BY DEVON 2026-08-03: 'RMS may morph the photo's shape'. Correct, and quantified.\n\nCURRENT MODEL. The photo horizon is corrected by alt - (c0 + A*cos az + B*sin az). That is a FIRST-ORDER approximation to a rigid rotation of the sphere. To first order it can only re-orient the horizon, not reshape it — but the higher-order term is not always negligible.\n\nMEASURED APPROXIMATION ERROR (approx minus exact rotation, max over azimuth):\n  tilt  4.8 deg (Mode A as fitted): 0.04 deg at alt 10, 0.11 at alt 30, 0.34 at alt 60\n  tilt 12.0 deg (Devon's deliberate pano tilt): 0.23 / 0.73 / 2.19\n  tilt 20.0 deg: 0.68 / 2.05 / 6.10\n\nWHY THIS MATTERS DESPITE MODE A BEING FINE. The error grows with BOTH tilt and altitude, and Mode B's documented instruction is to tilt up so the horizon sits inside the frame — the 14.15.23 panorama carried ~12 deg of intentional tilt. At alt 60 that is a 2.2 deg distortion, comparable to the whole RMS budget (2.2-2.8 deg), and it falls exactly on the tall tree, the feature that actually costs targets. The approximation degrades precisely in the configuration the tool tells users to produce.\n\nFIX. Replace the sinusoid with a true rotation: build the unit vector from (az, alt), apply a rotation matrix about the horizontal axis perpendicular to the tilt direction, read altitude back from the z component. Same three parameters (pitch, tiltA, tiltB -> magnitude + direction), no approximation, error zero at any tilt. Prototype verified in scratchpad.\n\nSECOND, INDEPENDENT ISSUE — OUTLIERS TIP THE WHOLE SPHERE. A rigid rotation cannot invent a bend in the horizon, but it can tip everything, so a single bad fiducial drags the global solution. This is not hypothetical: az 40 from the 2026-08-02 mask is very likely a false edge (see the too-low-ceiling ticket), and plain least squares will happily rotate the sphere to reduce its residual, corrupting every other azimuth. Use a robust loss (Huber or trimmed least squares) or explicit outlier rejection, and report per-fiducial leverage so a dominant point is visible rather than silent.","status":"open","priority":1,"issue_type":"bug","owner":"devon.jones@gmail.com","created_at":"2026-08-03T19:20:28.11962868-06:00","created_by":"Devon Jones","updated_at":"2026-08-03T19:20:28.11962868-06:00"}
